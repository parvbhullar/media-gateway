---
phase: 01-api-shell-cheap-wrappers
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/handler/api_v1/gateways.rs
  - src/console/handlers/sip_trunk.rs
  - tests/api_v1_gateways.rs
autonomous: true
requirements:
  - GWY-01
  - GWY-02
  - GWY-03
  - GWY-04
must_haves:
  truths:
    - "`POST /api/v1/gateways` with valid Bearer + body creates a `sip_trunk` row and returns 201 with `GatewayView` JSON matching docs/CARRIER-API.md §Gateways example"
    - "`POST /api/v1/gateways` with duplicate name returns 409 Conflict"
    - "`POST /api/v1/gateways` with invalid body (empty name, malformed proxy_addr) returns 400 Bad Request"
    - "`PUT /api/v1/gateways/{name}` on existing row updates it and returns 200 with `GatewayView`"
    - "`PUT /api/v1/gateways/{name}` on missing name returns 404"
    - "`DELETE /api/v1/gateways/{name}` hard-deletes and returns 204 when no DIDs/routes reference it"
    - "`DELETE /api/v1/gateways/{name}` returns 409 Conflict naming the first referencing DID or routing record (engagement tracking GWY-03)"
    - "`DELETE /api/v1/gateways/{name}` on missing name returns 404"
    - "On create success, gateway health monitoring is re-hooked by calling the existing proxy/gateway_health mechanism (GWY-04)"
    - "Console HTML page `/console/sip_trunks` still renders identically after pure-fn extraction (MIG-03 spot check)"
  artifacts:
    - path: "src/console/handlers/sip_trunk.rs"
      provides: "Pure data fns: create_sip_trunk_row, update_sip_trunk_row, delete_sip_trunk_row, count_engagements"
      contains: "pub(crate) async fn create_sip_trunk_row"
    - path: "src/handler/api_v1/gateways.rs"
      provides: "CreateGatewayRequest, UpdateGatewayRequest, 3 new handlers wired in router()"
      exports: ["create_gateway", "update_gateway", "delete_gateway"]
    - path: "tests/api_v1_gateways.rs"
      provides: "Extended integration tests covering write routes"
  key_links:
    - from: "src/handler/api_v1/gateways.rs"
      to: "src/console/handlers/sip_trunk.rs pure fns"
      via: "create_sip_trunk_row / update_sip_trunk_row / delete_sip_trunk_row"
      pattern: "create_sip_trunk_row|update_sip_trunk_row|delete_sip_trunk_row"
    - from: "delete_gateway handler"
      to: "engagement tracking queries"
      via: "count_did_references + count_routing_references pre-check"
      pattern: "count_engagements|reference"
    - from: "create_gateway handler"
      to: "proxy/gateway_health monitoring registration"
      via: "existing health-monitor registration helper"
      pattern: "gateway_health|health_monitor|register_trunk"
---

<objective>
Ship the 3 gateway write routes (`POST`, `PUT`, `DELETE /api/v1/gateways[/{name}]`) by extracting pure data fns from `console/handlers/sip_trunk.rs` and extending the existing `api_v1/gateways.rs` (which already ships `GET` + `trunk-test`). Includes engagement tracking for delete (GWY-03) and gateway health monitoring hook on create (GWY-04).

Purpose: Close the Gateways group to full CRUD so operators can provision carriers via REST without the console UI.

Output: 3 new handlers in `api_v1/gateways.rs`, 4 new pure fns in `console/handlers/sip_trunk.rs`, extended `tests/api_v1_gateways.rs`.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-shell-cheap-wrappers/01-CONTEXT.md
@.planning/phases/01-api-shell-cheap-wrappers/01-01-SUMMARY.md
@docs/plans/2026-04-14-phase-1-api-shell.md
@docs/CARRIER-API.md
@src/handler/api_v1/gateways.rs
@src/handler/api_v1/error.rs
@src/handler/api_v1/common.rs
@src/console/handlers/sip_trunk.rs
@src/console/handlers/did.rs
@src/proxy/gateway_health.rs
@src/models/sip_trunk.rs
@tests/api_v1_gateways.rs

<interfaces>
<!-- From Plan 01-01 output (already landed) -->
// ApiError::not_implemented / bad_request / conflict / not_found / internal all available
// PaginatedResponse<T>, Pagination in src/handler/api_v1/common.rs
// Adapter pattern proven: pub(crate) async fn foo(db: &DatabaseConnection, input) -> Result<T, DbErr>

// Existing GatewayView (src/handler/api_v1/gateways.rs:22-56) — REUSE, do not rewrite.
// Existing router() (line 58-63) — EXTEND with 3 routes.

// SeaORM Entity
// use crate::models::sip_trunk::{Entity as TrunkEntity, Column as TrunkColumn, Model as TrunkModel, ActiveModel};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract sip_trunk pure fns (create/update/delete + engagement tracking)</name>
  <files>
    src/console/handlers/sip_trunk.rs
  </files>
  <behavior>
    - `create_sip_trunk_row(db, input)` inserts a new row and returns it, or `DbErr::Exec` on duplicate name
    - `update_sip_trunk_row(db, name, input)` returns `None` if not found, `Some(model)` on success
    - `delete_sip_trunk_row(db, name)` returns `false` if not found, `true` on delete
    - `count_engagements(db, name) -> Result<Engagements, DbErr>` returns counts of DIDs with `trunk_name = name` and routing records referencing `name`. Struct shape: `{ did_count: u64, routing_count: u64, first_did: Option<String>, first_routing_target: Option<String> }`
    - Existing console HTML handlers for sip_trunks (create/update/delete forms) still render and redirect identically after refactor
  </behavior>
  <action>
    1. Read current sip_trunk console handlers — identify the SeaORM logic for POST form (create), PUT/PATCH form (update), DELETE form (delete).
    2. Extract each into module-level `pub(crate) async fn`:
       ```rust
       pub(crate) struct SipTrunkCreateInput { /* all fields the console form accepts */ }
       pub(crate) struct SipTrunkUpdateInput { /* partial fields */ }
       pub(crate) struct Engagements {
           pub did_count: u64,
           pub routing_count: u64,
           pub first_did: Option<String>,
           pub first_routing_target: Option<String>,
       }

       pub(crate) async fn create_sip_trunk_row(db: &DatabaseConnection, input: SipTrunkCreateInput) -> Result<TrunkModel, DbErr>;
       pub(crate) async fn update_sip_trunk_row(db: &DatabaseConnection, name: &str, input: SipTrunkUpdateInput) -> Result<Option<TrunkModel>, DbErr>;
       pub(crate) async fn delete_sip_trunk_row(db: &DatabaseConnection, name: &str) -> Result<bool, DbErr>;
       pub(crate) async fn count_engagements(db: &DatabaseConnection, name: &str) -> Result<Engagements, DbErr>;
       ```
    3. `count_engagements` queries:
       - DID table: `COUNT(*)` and `SELECT number LIMIT 1` where `trunk_name = name`
       - Routing table (if routing records reference trunk names in the current schema — check `src/models/routing*.rs` and the existing console routing handler to find the column). If routing doesn't model trunk refs yet in Phase 1 (because routing CRUD is deferred to Phase 6), still scan known routing storage for literal `name` references. If no routing table exists in Phase 1 context, implement the DID check only and TODO-comment the routing side with `// TODO(Phase 6): add routing reference check once routing records sub-route lands`.
    4. Refactor the existing console HTML handlers to call the new pure fns. Their outer signature (`State<Arc<ConsoleState>>`, `AuthRequired`, form extractors) must not change. No template or redirect change.
    5. Write unit tests in-module or a sibling test file for the pure fns against an in-memory sqlite test DB (use existing test-db helper pattern from other console modules if present).
  </action>
  <verify>
    <automated>cargo build -p media-gateway --all-targets &amp;&amp; cargo test -p media-gateway console::handlers::sip_trunk &amp;&amp; cargo clippy -p media-gateway --all-targets -- -D warnings</automated>
  </verify>
  <done>
    - 4 pure fns exposed as `pub(crate)` in `sip_trunk.rs`
    - Console HTML create/update/delete still work (manual MIG-03 spot check on `/console/sip_trunks`)
    - Existing `tests/gateway_health_unit.rs` and `tests/gateway_health_probe.rs` still pass (regression)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire 3 write handlers into api_v1/gateways.rs with engagement tracking + health hook + tests</name>
  <files>
    src/handler/api_v1/gateways.rs,
    tests/api_v1_gateways.rs
  </files>
  <behavior>
    - `POST /api/v1/gateways` body `CreateGatewayRequest` → 201 + `GatewayView`; duplicate name → 409; missing required fields → 400
    - `PUT /api/v1/gateways/{name}` body `UpdateGatewayRequest` → 200 + `GatewayView`; missing → 404; bad body → 400
    - `DELETE /api/v1/gateways/{name}` → 204 when no engagement; 409 with body `{"error":"gateway 'X' is referenced by DID '+1555...'", ...}` when DID/routing references exist; 404 if missing
    - After `POST` success, gateway health monitoring is scheduled for the new row (GWY-04) — calls existing registration helper in `proxy/gateway_health.rs`
    - All 4 existing (GET list / GET one / trunk-test / auth) tests in `tests/api_v1_gateways.rs` still pass
  </behavior>
  <action>
    1. Extend [src/handler/api_v1/gateways.rs](../../../src/handler/api_v1/gateways.rs):
       - Add `CreateGatewayRequest` and `UpdateGatewayRequest` structs (`#[derive(Deserialize)]`). Cross-check field names against docs/CARRIER-API.md §Gateways create example. Map them to `SipTrunkCreateInput` / `SipTrunkUpdateInput`.
       - Add 3 handlers:
         ```rust
         async fn create_gateway(State(state): State<AppState>, Json(req): Json<CreateGatewayRequest>) -> ApiResult<(StatusCode, Json<GatewayView>)>
         async fn update_gateway(State(state): State<AppState>, Path(name): Path<String>, Json(req): Json<UpdateGatewayRequest>) -> ApiResult<Json<GatewayView>>
         async fn delete_gateway(State(state): State<AppState>, Path(name): Path<String>) -> ApiResult<StatusCode>
         ```
       - `create_gateway` validates (non-empty name, valid direction, valid transport) → `bad_request` on failure, then calls `create_sip_trunk_row`. On `DbErr` containing unique-violation, return `ApiError::conflict`. On success, call the existing gateway-health registration/subscription function (look for `GatewayHealthMonitor::register_trunk` or equivalent on `state.sip_server()` — check `src/proxy/gateway_health.rs` for the exact API). Return `(StatusCode::CREATED, Json(GatewayView::from(model)))`.
       - `update_gateway` maps `None` → `not_found`, `Some(model)` → 200.
       - `delete_gateway` — FIRST call `count_engagements(db, &name)`. If `did_count > 0` or `routing_count > 0`, return `ApiError::conflict(format!("gateway '{}' is referenced by DID '{}' / routing '{}'", name, first_did.unwrap_or_default(), first_routing_target.unwrap_or_default()))`. Otherwise call `delete_sip_trunk_row`; `false` → 404; `true` → `StatusCode::NO_CONTENT`.
       - Extend `router()` with `.route("/gateways", post(create_gateway))`, `.route("/gateways/{name}", put(update_gateway).delete(delete_gateway))`. Use axum method-merging so GET/PUT/DELETE share the same path.

    2. Extend [tests/api_v1_gateways.rs](../../../tests/api_v1_gateways.rs) — add test cases following the existing harness pattern. IT-01 requires 4 cases per route:
       - `create_gateway_401` / `create_gateway_happy` / `create_gateway_409_duplicate` / `create_gateway_400_bad_body`
       - `update_gateway_401` / `update_gateway_happy` / `update_gateway_404_missing` / `update_gateway_400_bad_body`
       - `delete_gateway_401` / `delete_gateway_happy` / `delete_gateway_404_missing` / `delete_gateway_409_referenced_by_did`
       - Engagement conflict test: seed 1 DID with `trunk_name = "carrier-a"`, attempt `DELETE /api/v1/gateways/carrier-a`, assert 409 + body contains `"+1..."`.
       - Happy-path create test: assert the new row exists via `GET /api/v1/gateways/{name}` after create AND assert response body matches docs/CARRIER-API.md §Gateways shape (structural compare).
  </action>
  <verify>
    <automated>cargo build -p media-gateway --all-targets &amp;&amp; cargo test -p media-gateway --test api_v1_gateways &amp;&amp; cargo clippy -p media-gateway --all-targets -- -D warnings</automated>
  </verify>
  <done>
    - 3 write handlers live behind Bearer auth at `/api/v1/gateways[/{name}]`
    - Engagement tracking blocks delete with 409 when referenced (GWY-03)
    - Gateway health monitoring re-hooked on create (GWY-04)
    - ≥12 new test cases across the 3 write routes pass
    - `cargo test --test api_v1_gateways` green; clippy clean
  </done>
</task>

</tasks>

<verification>
- `cargo test -p media-gateway --test api_v1_gateways` all green
- `cargo clippy` clean
- Manual MIG-03 check: `/console/sip_trunks` create/edit/delete forms still work
- `GatewayView` JSON shape matches docs/CARRIER-API.md §Gateways example
</verification>

<success_criteria>
- Full Gateways CRUD available at `/api/v1/gateways`
- Engagement tracking prevents orphan references
- Health monitoring wired on create
- IT-01 coverage for all 3 write routes
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-shell-cheap-wrappers/01-02-SUMMARY.md` with:
- Final `CreateGatewayRequest` / `UpdateGatewayRequest` field list
- Engagement tracking SQL/ORM queries used
- How gateway health hook was re-invoked on create
- MIG-03 spot-check result for `/console/sip_trunks`
</output>
</content>
</invoke>