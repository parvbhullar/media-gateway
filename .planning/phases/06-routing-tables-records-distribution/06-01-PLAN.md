---
phase: 06-routing-tables-records-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/routing_tables.rs
  - src/models/mod.rs
  - src/models/migration.rs
  - src/handler/api_v1/routing_tables.rs
  - src/handler/api_v1/routing_records.rs
  - src/handler/api_v1/mod.rs
  - src/config.rs
autonomous: true
requirements: [RTE-01, RTE-02]
tags: [routing, schema, migration, scaffolding, phase6]
must_haves:
  truths:
    - "Migrator registers exactly one new Phase-6 migration that creates `supersip_routing_tables` (D-01)"
    - "Two stub sub-routers `routing_tables::router()` and `routing_records::router()` are merged into `/api/v1` and respond with 501 Not Implemented for every documented route until Waves 2/3 land"
    - "RouteResult / resolve response carries a `matched_record_id: Option<String>` field so Wave 3 can populate it (D-30 extension)"
    - "Legacy `rustpbx_routes` table and `src/models/routing.rs` are NOT modified (D-05)"
    - "`cargo check -p rustpbx --lib` compiles green; `cargo test -p rustpbx --lib` baseline passes with no regressions"
  artifacts:
    - path: src/models/routing_tables.rs
      provides: "SeaORM entity + Migration for supersip_routing_tables (records: Json column, name UNIQUE)"
      contains: "table_name = \"supersip_routing_tables\""
    - path: src/handler/api_v1/routing_tables.rs
      provides: "Stub router() exposing GET/POST /routing/tables and GET/PUT/DELETE /routing/tables/{name} as 501 placeholders"
      exports: ["router"]
    - path: src/handler/api_v1/routing_records.rs
      provides: "Stub router() exposing GET/POST /routing/tables/{name}/records and GET/PUT/DELETE /routing/tables/{name}/records/{record_id} as 501 placeholders"
      exports: ["router"]
    - path: src/handler/api_v1/mod.rs
      provides: "Module declarations + .merge(routing_tables::router()) + .merge(routing_records::router())"
    - path: src/config.rs
      provides: "RouteTrace (or ResolveRouteResponse-feeding type) extended with matched_record_id: Option<String>"
  key_links:
    - from: "src/models/migration.rs::Migrator::migrations"
      to: "src/models/routing_tables.rs::Migration"
      via: "Box::new(super::routing_tables::Migration) appended last"
      pattern: "routing_tables::Migration"
    - from: "src/handler/api_v1/mod.rs::api_v1_router"
      to: "routing_tables::router(), routing_records::router()"
      via: "protected.merge(...) chain"
      pattern: "\\.merge\\(routing_(tables|records)::router\\(\\)\\)"
---

<objective>
Lay the schema + scaffolding foundation for Phase 6:
1. Create the new `supersip_routing_tables` table (single row per table, records embedded as a JSON array per D-01).
2. Register stub sub-routers for `/api/v1/routing/tables[/...]` and `/api/v1/routing/tables/{name}/records[/...]` so Waves 2 + 3 can replace the bodies without touching `mod.rs` (Phase 5 file-ownership lesson).
3. Extend the resolve response surface (`RouteTrace` and/or `ResolveRouteResponse`) with `matched_record_id: Option<String>` so Wave 3 has somewhere to write the matched record's UUID per D-30.

Purpose: Wave 1 is the only wave allowed to edit `src/handler/api_v1/mod.rs` and `src/models/migration.rs`. Wave 2 (06-02, 06-03) and Wave 3 (06-04) operate on disjoint files.

Output: Empty entity, single forward-only migration, two 501-stub routers wired into the api_v1 surface, and a `matched_record_id` field threaded through the resolve response struct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md
@.planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-04-SUMMARY.md
@src/models/migration.rs
@src/models/trunk_acl_entries.rs
@src/handler/api_v1/mod.rs
@src/handler/api_v1/routing.rs
@src/handler/api_v1/trunk_acl.rs
@src/config.rs

<interfaces>
<!-- Patterns the executor MUST mirror. -->

From src/models/trunk_acl_entries.rs (entity + Migration in same file pattern):
```rust
#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "supersip_trunk_acl_entries")]
pub struct Model { ... }

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation { ... }

impl ActiveModelBehavior for ActiveModel {}

// ─── Migration ───────────────────────────────────────────────────────────────
pub struct Migration;
#[async_trait::async_trait]
impl MigrationTrait for Migration { ... }
```

From src/handler/api_v1/mod.rs:
```rust
pub mod routing;                  // Phase 3 — RTE-03
// new in Phase 6:
pub mod routing_tables;
pub mod routing_records;

let protected: Router<AppState> = Router::new()
    .merge(routing::router())     // Phase 3 — RTE-03 (existing)
    .merge(routing_tables::router())   // Phase 6 — RTE-01
    .merge(routing_records::router())  // Phase 6 — RTE-02
    ;
```

From src/handler/api_v1/routing.rs (existing ResolveRouteResponse — extend with matched_record_id):
```rust
pub struct ResolveRouteResponse {
    pub result: String,
    pub matched_table: Option<String>,
    pub matched_record_index: Option<i32>,
    // Phase 6 ADD: pub matched_record_id: Option<String>,
    pub match_reason: Option<String>,
    pub target: Option<ResolveTarget>,
    pub selected_gateway: Option<String>,
    pub trace: Vec<serde_json::Value>,
}
```

From src/proxy/routing/matcher.rs::RouteTrace (Wave 3 will extend; Wave 1 only touches if needed for matched_record_id plumbing):
```rust
pub struct RouteTrace {
    pub matched_rule: Option<String>,
    pub selected_trunk: Option<String>,
    pub trunk_group_name: Option<String>,
    pub used_default_route: bool,
    pub rewrite_operations: Vec<String>,
    pub abort: Option<RouteAbortTrace>,
    // Phase 6 Wave 1 ADD: pub matched_record_id: Option<String>,
    // Phase 6 Wave 1 ADD: pub matched_table: Option<String>,
    // Phase 6 Wave 1 ADD: pub matched_record_index: Option<i32>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: New SeaORM entity + Migration for supersip_routing_tables</name>
  <files>src/models/routing_tables.rs, src/models/mod.rs, src/models/migration.rs</files>
  <read_first>
    - src/models/trunk_acl_entries.rs (entity+Migration colocated pattern)
    - src/models/trunk_capacity.rs (UNIQUE column convention; supersip_ prefix)
    - src/models/migration.rs (registration order; comment style)
    - src/models/mod.rs (pub mod listing)
    - .planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md §D-01 (table shape) and §"Migration registration order"
  </read_first>
  <behavior>
    - DDL creates `supersip_routing_tables` with columns: `id BIGSERIAL PK`, `name TEXT UNIQUE NOT NULL`, `description TEXT NULL`, `direction TEXT NOT NULL DEFAULT 'both'`, `priority INTEGER NOT NULL DEFAULT 100`, `is_active BOOLEAN NOT NULL DEFAULT true`, `records JSON NOT NULL DEFAULT '[]'`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`.
    - Test: `cargo test -p rustpbx --lib models::routing_tables` compiles (even with zero unit tests this confirms entity wiring).
    - Test: `Migrator::migrations()` returns the new migration as the LAST element (verified by a unit test in this file: `assert!(matches!(Migrator::migrations().last(), Some(_)))` — name-comparison via `name()` accessor).
  </behavior>
  <action>
    1. CREATE `src/models/routing_tables.rs`:
       - Mirror `src/models/trunk_acl_entries.rs` layout (entity + `pub struct Migration` + `impl MigrationTrait for Migration`).
       - `#[sea_orm(table_name = "supersip_routing_tables")]`.
       - Columns: `id: i64 (PK auto)`, `name: String`, `description: Option<String>`, `direction: String` (enum stored as text), `priority: i32`, `is_active: bool`, `records: serde_json::Value` (use `Json` SeaORM type), `created_at: DateTimeUtc`, `updated_at: DateTimeUtc`.
       - Add UNIQUE index on `name` via `Index::create().name("idx_supersip_routing_tables_name").table(...).col(Column::Name).unique()`.
       - `impl ActiveModelBehavior for ActiveModel {}` — empty.
       - Migration `name()` returns `"m20260426_000001_create_supersip_routing_tables"` (or matching the project's existing pattern).
    2. EDIT `src/models/mod.rs`: add `pub mod routing_tables;` (alphabetical or grouped per existing convention).
    3. EDIT `src/models/migration.rs`: append `Box::new(super::routing_tables::Migration)` to the `vec![...]` list in `Migrator::migrations`. Add a comment block above the line that reads:
       ```
       // Phase 6 Plan 06-01 — RTE-01/RTE-02 routing tables (records embedded as JSON column per D-01).
       // Forward-only. Legacy rustpbx_routes is intentionally untouched (D-05).
       ```
    4. Do NOT touch `src/models/routing.rs` (legacy).
    5. Run `cargo check -p rustpbx --lib` — must be clean.
  </action>
  <verify>
    <automated>cargo check -p rustpbx --lib 2>&1 | tail -5 | grep -qE "(Finished|warning: unused)" &amp;&amp; grep -q "routing_tables::Migration" src/models/migration.rs &amp;&amp; grep -q "table_name = \"supersip_routing_tables\"" src/models/routing_tables.rs</automated>
  </verify>
  <done>
    - `src/models/routing_tables.rs` exists with the entity + Migration block.
    - `Migrator::migrations()` last entry is `routing_tables::Migration`.
    - `src/models/routing.rs` (legacy) untouched (verified via `git diff src/models/routing.rs` is empty).
    - `cargo check -p rustpbx --lib` is clean.
  </done>
  <acceptance_criteria>
    - `grep -q "table_name = \"supersip_routing_tables\"" src/models/routing_tables.rs`
    - `grep -q "pub mod routing_tables;" src/models/mod.rs`
    - `grep -q "routing_tables::Migration" src/models/migration.rs`
    - `git diff --quiet src/models/routing.rs` (legacy file unchanged)
    - `cargo check -p rustpbx --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stub routers for /routing/tables and /routing/tables/{name}/records (501 placeholders) + mod.rs registration</name>
  <files>src/handler/api_v1/routing_tables.rs, src/handler/api_v1/routing_records.rs, src/handler/api_v1/mod.rs</files>
  <read_first>
    - src/handler/api_v1/mod.rs (router merge convention; see existing `routing::router()` line)
    - src/handler/api_v1/routing.rs (Phase 3 RTE-03 stub-then-impl pattern; preserve `pub fn router()` signature)
    - src/handler/api_v1/error.rs (ApiError::not_implemented variant — must exist per SHELL-03)
    - src/handler/api_v1/trunk_acl.rs (501-stub example for sub-resource that ships impl in same wave — for shape reference only)
    - .planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md §"Routes shipped" (8 endpoints) and §"Router wiring"
  </read_first>
  <behavior>
    - Hitting `GET /api/v1/routing/tables` with valid Bearer auth returns `501 Not Implemented` (until 06-02 lands).
    - Hitting any of the 5 records-route endpoints with valid Bearer auth returns `501 Not Implemented` (until 06-03 lands).
    - Hitting any of these endpoints without Bearer auth returns `401 Unauthorized` (proven by existing middleware — no test required).
    - The two `pub fn router()` signatures are stable across waves so 06-02 / 06-03 can replace handler bodies without re-touching `mod.rs`.
  </behavior>
  <action>
    1. CREATE `src/handler/api_v1/routing_tables.rs`:
       - Module doc-comment cites RTE-01 + D-27. Include this NOTE block (mirrors trunk_acl.rs):
         ```
         //! NOTE: This file is the Wave-1 STUB. Plan 06-02 replaces the
         //! handler bodies below — DO NOT change the `pub fn router()`
         //! signature or the route paths or 06-02 will need to re-touch
         //! `mod.rs` (forbidden by Phase 5 file-ownership invariant).
         ```
       - `pub fn router() -> Router&lt;AppState&gt;` registers:
         - `GET    /routing/tables`               -> `list_tables`
         - `POST   /routing/tables`               -> `create_table`
         - `GET    /routing/tables/{name}`        -> `get_table`
         - `PUT    /routing/tables/{name}`        -> `update_table`
         - `DELETE /routing/tables/{name}`        -> `delete_table`
       - Each handler body: `Err(ApiError::not_implemented("phase 6 plan 06-02"))`.
    2. CREATE `src/handler/api_v1/routing_records.rs` with same NOTE block citing 06-03 + D-28, registering:
         - `GET    /routing/tables/{name}/records`
         - `POST   /routing/tables/{name}/records`
         - `GET    /routing/tables/{name}/records/{record_id}`
         - `PUT    /routing/tables/{name}/records/{record_id}`
         - `DELETE /routing/tables/{name}/records/{record_id}`
       - Each handler body: `Err(ApiError::not_implemented("phase 6 plan 06-03"))`.
    3. EDIT `src/handler/api_v1/mod.rs`:
       - Add `pub mod routing_tables;` and `pub mod routing_records;` next to `pub mod routing;` with comment `// Phase 6 — RTE-01/RTE-02 (handlers in Plans 06-02 / 06-03)`.
       - Append `.merge(routing_tables::router())` and `.merge(routing_records::router())` to the `protected` chain (after the existing `.merge(routing::router())` line).
    4. Run `cargo check -p rustpbx --lib`. Expect clean.
    5. **CRITICAL invariant for Waves 2 + 3:** these are the LAST edits to `src/handler/api_v1/mod.rs` for Phase 6. Plans 06-02, 06-03, 06-04 MUST NOT modify this file.
  </action>
  <verify>
    <automated>grep -q "pub mod routing_tables;" src/handler/api_v1/mod.rs &amp;&amp; grep -q "pub mod routing_records;" src/handler/api_v1/mod.rs &amp;&amp; grep -q "routing_tables::router()" src/handler/api_v1/mod.rs &amp;&amp; grep -q "routing_records::router()" src/handler/api_v1/mod.rs &amp;&amp; grep -q "ApiError::not_implemented" src/handler/api_v1/routing_tables.rs &amp;&amp; grep -q "ApiError::not_implemented" src/handler/api_v1/routing_records.rs &amp;&amp; cargo check -p rustpbx --lib 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <done>
    - Both stub files exist; both `pub fn router()` exported.
    - `mod.rs` declares both modules and merges both routers.
    - Compile clean.
    - Comment in each stub instructs Wave 2/3 to preserve `router()` signature and avoid `mod.rs`.
  </done>
  <acceptance_criteria>
    - File exists: `src/handler/api_v1/routing_tables.rs`
    - File exists: `src/handler/api_v1/routing_records.rs`
    - `cargo check -p rustpbx --lib` exits 0
    - The string `routing_tables::router()` appears exactly once in `src/handler/api_v1/mod.rs`
    - The string `routing_records::router()` appears exactly once in `src/handler/api_v1/mod.rs`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend resolve response with matched_record_id (D-30 plumbing)</name>
  <files>src/handler/api_v1/routing.rs, src/proxy/routing/matcher.rs (RouteTrace only), src/config.rs (if RouteResult needs the field)</files>
  <read_first>
    - src/handler/api_v1/routing.rs (`ResolveRouteResponse` struct)
    - src/proxy/routing/matcher.rs (`RouteTrace` struct around line 26-36)
    - src/config.rs §`pub enum RouteResult` (line ~818) — confirm whether record_id needs to ride RouteResult or just RouteTrace
    - .planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md §D-30, §D-31 (resolve dry-run wiring)
    - .planning/phases/03-trunk-sub-resources-l1-and-routing-resolve/03-CONTEXT.md §RTE-03 (existing `matched_table` / `matched_record_index` left as None by Phase 3)
  </read_first>
  <behavior>
    - `ResolveRouteResponse` gains `pub matched_record_id: Option&lt;String&gt;` serialized as JSON field `matched_record_id`. Default at construction sites: `None`.
    - `RouteTrace` gains `pub matched_record_id: Option&lt;String&gt;` (and, if not already present and easier than threading through ResolveRouteResponse separately, `pub matched_table: Option&lt;String&gt;` + `pub matched_record_index: Option&lt;i32&gt;` — current `ResolveRouteResponse` already has the last two, so check before duplicating).
    - Test (RED): `tests/api_v1_routing_resolve.rs` (existing from Phase 3) or a new `tests/api_v1_routing_resolve_matched_record_id.rs` asserts the JSON response body contains the key `"matched_record_id"` with value `null` for an arbitrary unmatched dry-run. Should FAIL before Task 3 (key absent) and PASS after.
    - Test (GREEN): same test passes.
    - **No semantic change in this plan** — the field is wired but always `None`; Wave 3 (06-04) populates it.
  </behavior>
  <action>
    1. RED — Add a unit/integration test that POSTs `/api/v1/routing/resolve` with bogus caller/destination and asserts the response JSON contains the key `matched_record_id` (use serde_json `as_object().unwrap().contains_key("matched_record_id")`). Run; expect failure.
    2. EDIT `src/handler/api_v1/routing.rs`:
       - Add `pub matched_record_id: Option&lt;String&gt;` to `ResolveRouteResponse` (serde default = None — derive `Default` if not present, or explicitly serialize via `#[serde(default)]`).
       - At every construction site of `ResolveRouteResponse` in this file, set `matched_record_id: None`.
    3. EDIT `src/proxy/routing/matcher.rs`:
       - Add `pub matched_record_id: Option&lt;String&gt;` to the `RouteTrace` struct (with `#[serde(default)]` for backward-compat consumers). Default in `Default::default()` is already `None` since `Option` derives Default to None — verify.
       - DO NOT add new fields if they're already there; preserve existing `matched_table` / `matched_record_index` if RouteTrace already has them (it does NOT in Wave 1 — Phase 3 put them on `ResolveRouteResponse` only). Wave 3 will plumb them through; Wave 1 only adds `matched_record_id` to RouteTrace so Wave 3 has somewhere to write it.
    4. `src/config.rs::RouteResult` — only edit if `matched_record_id` cannot ride `RouteTrace`. Default branch: leave `RouteResult` untouched (matched_record_id rides RouteTrace, which is sibling output of `match_invite_with_trace`).
    5. GREEN — re-run the new test; expect pass.
    6. REFACTOR — none required; the change is purely additive.
  </action>
  <verify>
    <automated>grep -q "matched_record_id: Option&lt;String&gt;" src/handler/api_v1/routing.rs &amp;&amp; grep -q "matched_record_id: Option&lt;String&gt;" src/proxy/routing/matcher.rs &amp;&amp; cargo test -p rustpbx --test api_v1_routing_resolve 2>&1 | tail -5 | grep -qE "test result.*0 failed" &amp;&amp; cargo check -p rustpbx --lib 2>&1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <done>
    - `ResolveRouteResponse` and `RouteTrace` both expose `matched_record_id: Option&lt;String&gt;`.
    - At least one test asserts the key is present in the JSON response (value null is acceptable in this plan).
    - All existing tests still pass.
  </done>
  <acceptance_criteria>
    - `grep -c "matched_record_id" src/handler/api_v1/routing.rs` >= 2 (struct field + at least one constructor site)
    - `grep -q "matched_record_id" src/proxy/routing/matcher.rs`
    - `cargo test -p rustpbx --test api_v1_routing_resolve` returns exit 0
    - `cargo check -p rustpbx --lib` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → /api/v1/routing/tables[/...] | Bearer-authenticated operator input crosses into stub handlers (501 only in this plan) |
| migration runner → DB | New `supersip_routing_tables` DDL applied on startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `supersip_routing_tables.records` JSON column | mitigate | Forward-only migration; UNIQUE on `name`; column NOT NULL with default `'[]'`. Schema enforces shape only — content validation lives in 06-02 / 06-03 |
| T-06-01-02 | Information Disclosure | Stub 501 responses | accept | Body is a fixed string referencing the Plan ID; reveals only that the surface is unimplemented (already public via roadmap). No data leakage. |
| T-06-01-03 | Denial of Service | Migration on large existing DBs | accept | Single CREATE TABLE; no ALTER on existing tables. Negligible impact. |
| T-06-01-04 | Elevation of Privilege | Bypassing Bearer auth via new routes | mitigate | Both stub routers merge into the existing `protected` chain in `mod.rs` — middleware applies before route dispatch (Phase 1 SHELL-01 pattern; verified 401-without-auth in IT-01). |
| T-06-01-05 | Repudiation | Schema rollout traceability | mitigate | Migration `name()` includes plan ID `m{date}_create_supersip_routing_tables`; comment block in `migration.rs` cites RTE-01/RTE-02 + D-01. |
</threat_model>

<verification>
- `cargo check -p rustpbx --lib` clean.
- `cargo test -p rustpbx --lib` baseline (Phase 5 1154/1154) holds; one new test added by Task 3 passes.
- `cargo test -p rustpbx --test api_v1_routing_resolve` passes (existing Phase 3 tests + Task 3 addition).
- `git diff src/models/routing.rs` empty (legacy untouched per D-05).
- `grep -c "ApiError::not_implemented" src/handler/api_v1/routing_tables.rs src/handler/api_v1/routing_records.rs` returns 10 (5 stubs each).
</verification>

<success_criteria>
1. New entity `supersip_routing_tables` registered in `Migrator::migrations()` as the LAST element; legacy `rustpbx_routes` schema and entity untouched.
2. Two stub sub-routers merged into `/api/v1`; all 10 endpoints respond 501 (or 401 without auth).
3. `ResolveRouteResponse` and `RouteTrace` both carry `matched_record_id: Option<String>` (always None until Wave 3).
4. `mod.rs` and `migration.rs` are FROZEN for the rest of Phase 6 — no Wave 2 / Wave 3 plan touches them (file-ownership invariant).
</success_criteria>

<output>
After completion, create `.planning/phases/06-routing-tables-records-distribution/06-01-SUMMARY.md` recording:
- Migration name + position in `Migrator::migrations()`
- The two new module paths + the matched_record_id wire
- Explicit confirmation that `mod.rs` and `migration.rs` are frozen for the rest of Phase 6
</output>
