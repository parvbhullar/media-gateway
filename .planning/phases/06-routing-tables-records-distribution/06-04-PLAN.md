---
phase: 06-routing-tables-records-distribution
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/proxy/routing/match_types.rs
  - src/proxy/routing/table_matcher.rs
  - src/proxy/routing/mod.rs
  - src/proxy/routing/matcher.rs
  - src/handler/api_v1/routing.rs
  - tests/proxy_routing_match_types.rs
autonomous: true
requirements: [RTE-04, RTE-05]
tags: [routing, matcher, lpm, regex, http-query, ssrf, ssrf-runtime, default-record, next-table, IT-04, phase6]
must_haves:
  truths:
    - "Each of the 5 match types (Lpm longest-prefix, ExactMatch, Regex with cached compile, Compare with op∈{eq,lt,gt,in}, HttpQuery with reqwest 2s default timeout) resolves correctly end-to-end through `match_invite_with_trace` (D-08..D-12, RTE-04)"
    - "When no record matches, a record with `is_default: true` is returned; if no default exists, RouteResult::Abort is preserved (D-19, RTE-05)"
    - "`next_table` chains across up to 3 tables; revisiting an already-visited table aborts with reason `routing_loop_detected` (D-25)"
    - "HttpQuery failure modes (timeout, 5xx, connection error, malformed JSON, redirected to denylisted IP) FALL THROUGH to next record — never 503 (D-15)"
    - "HttpQuery runtime SSRF defense: each request re-validates URL scheme + host against the same denylist 06-03 enforces at write time (defends against DNS rebind, DB tampering)"
    - "`/api/v1/routing/resolve` populates `matched_table`, `matched_record_index`, AND `matched_record_id` from the new tables (D-30) — replacing Phase 3's always-None sentinels"
    - "RouteTrace events distinguish each match type: `LpmMatch{prefix}`, `ExactMatch{value}`, `RegexMatch{pattern}`, `CompareMatch{op,value}`, `HttpQueryMatch{url,latency_ms}`, `HttpQueryFailed{url,error}`, `DefaultRecordUsed{table}`, `NoMatch{table}` (D-31)"
    - "Direction filter honored at table level (inbound/outbound/both) per D-21; cross-table priority sort ASC per D-22; legacy `rustpbx_routes` is NOT consulted (D-06)"
    - "Fresh DB read of `supersip_routing_tables` per INVITE — no caching in v2.0 (D-29, mirrors Phase 5 D-17 ACL pattern)"
  artifacts:
    - path: src/proxy/routing/match_types.rs
      provides: "Per-variant evaluators: eval_lpm, eval_exact, eval_regex (with cached compile), eval_compare, eval_http_query (with reqwest, 2s default timeout, runtime SSRF re-check)"
      exports: ["eval_lpm", "eval_exact", "eval_regex", "eval_compare", "eval_http_query", "MatchOutcome"]
    - path: src/proxy/routing/table_matcher.rs
      provides: "Orchestrator: filter by direction, sort tables by priority, evaluate records in position order, default fallback, next_table chain (depth ≤ 3) with visited-set loop detection"
      exports: ["match_against_supersip_tables", "MatchedRecordInfo"]
    - path: src/proxy/routing/matcher.rs
      provides: "Extended match_invite_with_trace consults supersip_routing_tables (D-06)"
    - path: src/handler/api_v1/routing.rs
      provides: "/resolve handler populates matched_table, matched_record_index, matched_record_id from match_against_supersip_tables (D-30)"
    - path: tests/proxy_routing_match_types.rs
      provides: "IT-04 suite — 5 match types end-to-end + default fallback + next_table chain + chain-depth-3 loop detection + HttpQuery happy/timeout/5xx/SSRF fall-through"
      contains: "#[tokio::test]"
  key_links:
    - from: "src/proxy/routing/matcher.rs::match_invite_with_trace (and siblings)"
      to: "src/proxy/routing/table_matcher.rs::match_against_supersip_tables"
      via: "called BEFORE existing `routes` snapshot consultation; if it returns Some(target), short-circuit; if None and no default, fall through to legacy in-memory `routes` arg only if non-empty (transitional) — preferred path per D-06: legacy ignored"
      pattern: "match_against_supersip_tables"
    - from: "eval_http_query"
      to: "reqwest::Client + private_ip_or_loopback (re-imported from routing_records or duplicated)"
      via: "POST {url} with body {caller_number, destination_number, src_ip, headers}; default 2s timeout; SSRF re-check before sending"
      pattern: "reqwest::Client::builder|timeout"
    - from: "src/handler/api_v1/routing.rs::resolve_route"
      to: "match_against_supersip_tables"
      via: "Resolve calls the same function as production matcher to populate matched_table/index/id"
      pattern: "matched_record_id|matched_table"
---

<objective>
Wire the new `supersip_routing_tables` data into `match_invite_with_trace` and ship IT-04 integration tests for all 5 match types + default fallback + next_table chains.

Purpose: Without this plan, RTE-04 and RTE-05 are unmet — the new tables are operator-visible (06-02/06-03) but never consulted by dispatch. This plan flips the matcher source per D-06 and completes the resolve dry-run wiring per D-30.

Output: Two new modules under `src/proxy/routing/`, a small extension to `match_invite_with_trace`, the resolve-handler completion, and a 14-test IT-04 suite.

This is the highest-risk plan in Phase 6 — touches the proxy hot path. Apply Phase 5 D-17 fresh-DB-read pattern (no caching beyond per-INVITE regex compile cache).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md
@.planning/phases/06-routing-tables-records-distribution/06-01-PLAN.md
@.planning/phases/06-routing-tables-records-distribution/06-02-PLAN.md
@.planning/phases/06-routing-tables-records-distribution/06-03-PLAN.md
@.planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-04-SUMMARY.md
@src/proxy/routing/matcher.rs
@src/proxy/routing/mod.rs
@src/handler/api_v1/routing.rs
@src/handler/api_v1/routing_records.rs
@src/models/routing_tables.rs

<interfaces>
<!-- Public surfaces for executor. -->

```rust
// src/proxy/routing/match_types.rs
pub enum MatchOutcome {
    Hit { reason: String },         // matched; reason added to RouteTrace events
    Miss,                            // didn't match
    Skip,                            // record inactive or default-flagged (skipped during normal eval)
}

pub fn eval_lpm(prefix: &str, destination: &str) -> MatchOutcome;
pub fn eval_exact(value: &str, destination: &str) -> MatchOutcome;
pub fn eval_regex(pattern: &str, destination: &str, cache: &mut RegexCache) -> MatchOutcome;
pub fn eval_compare(op: CompareOp, value: &CompareValue, destination: &str) -> MatchOutcome;
pub async fn eval_http_query(
    client: &reqwest::Client,
    url: &str,
    timeout_ms: Option<u32>,
    headers: Option<&HashMap<String,String>>,
    body: &HttpQueryBody,
) -> (MatchOutcome, Option<RoutingTarget>);

pub struct HttpQueryBody {
    pub caller_number: String,
    pub destination_number: String,
    pub src_ip: Option<String>,
    pub headers: HashMap<String,String>,
}
```

```rust
// src/proxy/routing/table_matcher.rs
pub struct MatchedRecordInfo {
    pub table_name: String,
    pub record_id: String,
    pub position: i32,
    pub target: RoutingTarget,
    pub used_default: bool,
}

pub async fn match_against_supersip_tables(
    db: &sea_orm::DatabaseConnection,
    http_client: &reqwest::Client,
    direction: DialDirection,           // inbound/outbound
    caller_number: &str,
    destination_number: &str,
    src_ip: Option<&str>,
    headers: &HashMap<String,String>,
    trace: &mut RouteTrace,
) -> Result<Option<MatchedRecordInfo>>;
```

```rust
// src/handler/api_v1/routing.rs (extension, NOT replacement)
// In resolve_route, after match_invite_with_trace returns:
if let Some(info) = trace.matched_table_info.as_ref() {
    response.matched_table = Some(info.table_name.clone());
    response.matched_record_index = Some(info.position);
    response.matched_record_id = Some(info.record_id.clone());
}
```

From src/proxy/routing/matcher.rs::RouteTrace (Plan 06-01 added matched_record_id; this plan extends with `matched_table` and `matched_record_index` if not already present, plus the typed events vector):
```rust
pub struct RouteTrace {
    pub matched_rule: Option<String>,
    pub selected_trunk: Option<String>,
    pub trunk_group_name: Option<String>,
    pub used_default_route: bool,
    pub rewrite_operations: Vec<String>,
    pub abort: Option<RouteAbortTrace>,
    pub matched_record_id: Option<String>,    // 06-01
    pub matched_table: Option<String>,         // 06-04 ADD if missing
    pub matched_record_index: Option<i32>,     // 06-04 ADD if missing
    pub events: Vec<serde_json::Value>,        // 06-04 ADD: typed events per D-31
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement match_types.rs (5 per-variant evaluators) with unit tests</name>
  <files>src/proxy/routing/match_types.rs, src/proxy/routing/mod.rs</files>
  <read_first>
    - src/proxy/routing/mod.rs (module registration pattern; how `codec_normalize` was added in Phase 5)
    - src/handler/api_v1/routing_records.rs (RoutingMatch, CompareOp, CompareValue, RoutingTarget — re-exported here, NOT redefined)
    - .planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md §D-08..D-12 (per-variant semantics), §D-13..D-17 (HttpQuery wire shape + failure semantics), §D-31 (RouteTrace events)
  </read_first>
  <behavior>
    Unit tests in `mod tests` of `match_types.rs`:
    - `lpm_longest_prefix_wins`: against `+14155551234`, prefixes `+1`, `+1415`, `+1416` — `+1415` wins.
    - `lpm_no_match`: prefix `+44` against `+1...` → Miss.
    - `exact_case_sensitive`: `Match` only on exact equality (D-09 case-sensitive).
    - `regex_compile_caching`: same pattern called 1000 times — `RegexCache` returns same Regex (verify hit count if exposed; or just assert single compile via instrumentation).
    - `regex_invalid_treated_as_miss_with_warn`: invalid pattern → Miss + log warning (defense-in-depth even though 06-03 validates at write).
    - `compare_eq_lt_gt`: digit-count comparisons.
    - `compare_in_range`: `[7, 15]` matches digit count 11.
    - `http_query_happy_path`: spin a `wiremock` server returning `{"matched": true, "target": {"kind": "trunk_group", "name": "us"}}` — eval returns `(Hit, Some(target))`.
    - `http_query_timeout_falls_through`: server delays > timeout_ms; eval returns `(Miss, None)`, NO panic.
    - `http_query_5xx_falls_through`: returns `Miss`.
    - `http_query_malformed_json_falls_through`: returns `Miss`.
    - `http_query_matched_false_falls_through`: server returns `{"matched": false}`; eval returns `Miss`.
    - `http_query_runtime_ssrf_localhost_falls_through`: url `http://127.0.0.1` — runtime SSRF check rejects BEFORE sending; returns `Miss` with HttpQueryFailed event reason `ssrf_blocked`.
  </behavior>
  <action>
    1. CREATE `src/proxy/routing/match_types.rs`:
       - `pub use crate::handler::api_v1::routing_records::{RoutingMatch, RoutingTarget, CompareOp, CompareValue};` (single source of truth; do NOT redefine).
       - Define `pub struct RegexCache { inner: HashMap<String, Regex> }` with `get_or_compile(&mut self, pat: &str) -> Option<&Regex>` (None on compile failure).
       - Implement `eval_lpm`, `eval_exact`, `eval_regex`, `eval_compare`, `eval_http_query` per D-08..D-12.
       - For `eval_http_query`:
         - Re-implement (or import from `routing_records`) `private_ip_or_loopback`. RECOMMEND: extract that helper into a new tiny module `src/handler/api_v1/ssrf.rs` and `pub use` from both call sites — avoids duplication. (If extraction adds churn, duplicate the function with a TODO comment citing this plan.)
         - Build `reqwest::Client` once at process start (or pass in as argument from server.rs); apply `timeout(Duration::from_millis(timeout_ms.unwrap_or(2000) as u64))`.
         - POST body: `{caller_number, destination_number, src_ip, headers}` per D-13.
         - Parse response `{matched: bool, target: RoutingTarget}` per D-14.
         - Failure modes (timeout, 5xx, connection, malformed JSON, ssrf_blocked) → `(Miss, None)` + warn log per D-15.
       - Module-level `mod tests` with the 12+ test cases above. Use `wiremock` for HTTP fixtures (add to dev-deps if missing).
    2. EDIT `src/proxy/routing/mod.rs`: `pub mod match_types;` (mirror `pub mod codec_normalize;` Phase 5 pattern).
    3. Run `cargo test -p rustpbx --lib proxy::routing::match_types` — all unit tests GREEN.
  </action>
  <verify>
    <automated>cargo test -p rustpbx --lib proxy::routing::match_types 2>&amp;1 | tail -3 | grep -qE "test result.*ok\\..*0 failed" &amp;&amp; grep -q "pub mod match_types;" src/proxy/routing/mod.rs &amp;&amp; grep -q "fn eval_http_query" src/proxy/routing/match_types.rs</automated>
  </verify>
  <done>
    - All 5 evaluators implemented; ≥12 unit tests passing.
    - HttpQuery runtime SSRF re-check verified by test.
    - `mod.rs` registers the new module.
  </done>
  <acceptance_criteria>
    - `[ -f src/proxy/routing/match_types.rs ]`
    - `grep -c "pub fn eval_" src/proxy/routing/match_types.rs` >= 4 (eval_lpm/exact/regex/compare; eval_http_query is async fn)
    - `grep -q "pub async fn eval_http_query" src/proxy/routing/match_types.rs`
    - `grep -q "pub mod match_types;" src/proxy/routing/mod.rs`
    - `cargo test -p rustpbx --lib proxy::routing::match_types` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement table_matcher.rs orchestrator (direction filter + priority sort + records eval + default + next_table chain depth 3) with unit tests</name>
  <files>src/proxy/routing/table_matcher.rs, src/proxy/routing/mod.rs, src/proxy/routing/matcher.rs</files>
  <read_first>
    - src/proxy/routing/match_types.rs (Task 1 output)
    - src/models/routing_tables.rs (Entity)
    - src/proxy/routing/matcher.rs (existing match_invite_with_trace + Phase 5 apply_phase5_gates pattern; locate insertion point BEFORE existing `routes` consultation per D-06)
    - .planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md §D-19..D-25, §D-29 (fresh DB read), §D-31 (events)
  </read_first>
  <behavior>
    Unit tests in `mod tests` of `table_matcher.rs` (use in-memory SQLite via `tests/common`):
    - `inbound_table_skipped_for_outbound_direction` (D-21)
    - `tables_sorted_by_priority_asc_first_match_wins` (D-22)
    - `lpm_records_compete_within_table_longest_wins` (D-23)
    - `inactive_record_skipped` (`is_active: false`)
    - `default_record_returned_when_no_match` (D-19, RTE-05)
    - `no_match_no_default_returns_none`
    - `next_table_chain_depth_2_resolves_target` — table A `next_table -> B`, B has Lpm hit
    - `next_table_chain_depth_3_caps` — table A → B → C, all hit; depth 3 OK
    - `next_table_chain_loop_detected` — A → B → A, returns `MatchOutcome` indicating `routing_loop_detected` and aborts; trace.abort.reason set.
    - `next_table_target_missing_logs_and_aborts` (best-effort write-time validation per D-26 means runtime missing target is possible)
  </behavior>
  <action>
    1. CREATE `src/proxy/routing/table_matcher.rs`:
       - `pub async fn match_against_supersip_tables(...)` (signature in interfaces block).
       - Algorithm:
         a. SeaORM: `Entity::find().filter(direction in [requested_direction, "both"]).filter(is_active = true).order_by_asc(priority).all(db)` — fresh per call (D-29).
         b. For each table: parse `records: serde_json::Value` into `Vec<RoutingRecord>`. Skip inactive records.
         c. Two-pass evaluation per table:
            - Pass 1: separate Lpm records from non-Lpm; evaluate all Lpm together, longest-prefix-wins (D-23).
            - Pass 2: if no Lpm hit, walk non-Lpm records in `position` ASC order; first hit wins.
         d. If no record matches in this table, look for `is_default: true` record; return its target.
         e. If target is `NextTable { name }` and `visited.insert(name).is_some()` → loop detected; trace `routing_loop_detected`, return Err / Some-with-abort.
         f. If target is `NextTable` and `visited.len() < 3`, recurse into named table.
         g. If target is `NextTable` past depth 3 → abort.
         h. Push `RouteTrace::events` entries per D-31 for each evaluation step.
    2. EDIT `src/proxy/routing/mod.rs`: `pub mod table_matcher;`.
    3. EDIT `src/proxy/routing/matcher.rs`:
       - Locate `match_invite_with_trace` (line ~168) and its sibling `match_invite_with_trace_and_codecs` (line ~200).
       - Insert a call to `match_against_supersip_tables(...)` BEFORE the existing `routes` snapshot consultation. If it returns `Some(MatchedRecordInfo)`, populate `trace.matched_table`, `trace.matched_record_index`, `trace.matched_record_id` and dispatch the target (TrunkGroup / Gateway / NextTable handled internally / Reject → existing `RouteResult::Reject` from Phase 5).
       - If it returns None AND legacy `routes` arg is empty/None → return `RouteResult::Abort` (no change).
       - **Per D-06: legacy `routes` arg becomes a no-op input.** Document in module doc-comment that the in-memory `routes` source is preserved only for backwards compatibility with non-DB call sites (tests, dry-run inspection without DB) and is NOT consulted when DB is available.
       - Add `matched_table`, `matched_record_index`, `events` fields to `RouteTrace` if missing (06-01 added `matched_record_id` only).
    4. Run `cargo test -p rustpbx --lib proxy::routing::table_matcher` and `cargo test -p rustpbx --lib proxy::routing::matcher` — all green.
    5. Run full `cargo test -p rustpbx --lib` — Phase 5 baseline (1154+) must hold; if any matcher tests regress because legacy `routes` is no longer consulted in DB-present mode, update the assertions to seed via `supersip_routing_tables` instead. Document deltas in summary.
  </action>
  <verify>
    <automated>cargo test -p rustpbx --lib proxy::routing::table_matcher 2>&amp;1 | tail -3 | grep -qE "0 failed" &amp;&amp; grep -q "match_against_supersip_tables" src/proxy/routing/matcher.rs &amp;&amp; grep -q "pub mod table_matcher;" src/proxy/routing/mod.rs &amp;&amp; cargo check -p rustpbx --lib 2>&amp;1 | tail -3 | grep -q "Finished"</automated>
  </verify>
  <done>
    - `match_against_supersip_tables` implemented with all 10 unit tests passing.
    - `match_invite_with_trace` consults supersip tables BEFORE legacy `routes`; legacy is silent in DB-present mode (D-06).
    - Loop detection via visited-set; depth cap = 3.
    - RouteTrace gains `matched_table`, `matched_record_index`, `events` if not already present.
  </done>
  <acceptance_criteria>
    - `[ -f src/proxy/routing/table_matcher.rs ]`
    - `grep -q "pub async fn match_against_supersip_tables" src/proxy/routing/table_matcher.rs`
    - `grep -q "routing_loop_detected" src/proxy/routing/table_matcher.rs`
    - `grep -q "match_against_supersip_tables" src/proxy/routing/matcher.rs`
    - `cargo test -p rustpbx --lib proxy::routing` exits 0
    - `cargo test -p rustpbx --lib` exits 0 (no regressions; deltas documented in summary)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire /resolve to populate matched_table/index/id + IT-04 integration suite</name>
  <files>src/handler/api_v1/routing.rs, tests/proxy_routing_match_types.rs</files>
  <read_first>
    - src/handler/api_v1/routing.rs (Phase 3 /resolve handler — `ResolveRouteResponse` already has `matched_table` and `matched_record_index`; 06-01 added `matched_record_id`)
    - src/proxy/routing/table_matcher.rs (Task 2 output)
    - .planning/phases/06-routing-tables-records-distribution/06-CONTEXT.md §D-30, §"Test convention (IT-01)" + IT-04 list (5 match types end-to-end + default fallback + next_table chain + chain-depth-3 loop detection + HttpQuery happy/timeout/5xx)
    - tests/api_v1_routing_resolve.rs (Phase 3) for harness reference
  </read_first>
  <behavior>
    `tests/proxy_routing_match_types.rs` cases (each `#[tokio::test]`):

    Match types (D-08..D-12):
    1. `it04_lpm_hit_via_resolve` — seed table with 3 Lpm records; POST /resolve; assert response `matched_table`, `matched_record_index`, `matched_record_id`, `target` correct.
    2. `it04_exact_match_hit_via_resolve`
    3. `it04_regex_hit_via_resolve` — pattern `^\\+1[0-9]{10}$`
    4. `it04_compare_eq_hit_via_resolve` — digit count = 11
    5. `it04_compare_in_range_hit_via_resolve`
    6. `it04_http_query_hit_via_resolve` — uses `wiremock` server returning `{matched:true, target:{kind:trunk_group, name:"x"}}`

    Fallback + chain (D-19, D-25, RTE-05):
    7. `it04_default_record_returned_when_no_match` — table with one Lpm record (no match) + one default — default's target wins; `RouteTrace::events` includes `DefaultRecordUsed{table}`.
    8. `it04_no_match_no_default_returns_abort`
    9. `it04_next_table_chain_resolves_via_chained_table` — table A `next_table -> B`, B matches.
    10. `it04_next_table_chain_loop_detected_returns_abort` — A → B → A; abort.reason = `routing_loop_detected`.
    11. `it04_next_table_chain_depth_3_caps`

    HttpQuery failure modes (D-15):
    12. `it04_http_query_timeout_falls_through_to_default` — wiremock delays > timeout; default record wins.
    13. `it04_http_query_5xx_falls_through`
    14. `it04_http_query_malformed_json_falls_through`
    15. `it04_http_query_runtime_ssrf_falls_through` — record has url `http://127.0.0.1` (operator sneaked it in via direct DB write or pre-validation bypass); runtime SSRF check rejects BEFORE sending HTTP.

    Resolve wiring (D-30):
    16. `it04_resolve_response_matched_record_id_is_uuid_v4` — assert `matched_record_id` is a parseable UUID v4.

    Direction filter + priority + legacy isolation (D-06, D-21, D-22):
    17. `it04_inbound_only_table_skipped_for_outbound_direction`
    18. `it04_legacy_rustpbx_routes_not_consulted_when_supersip_table_matches` — seed BOTH legacy and new; assert match comes from new (matched_table populated, not None).
  </behavior>
  <action>
    1. EDIT `src/handler/api_v1/routing.rs`:
       - In `resolve_route`, after calling the matcher, copy `trace.matched_table`, `trace.matched_record_index`, `trace.matched_record_id` into the response. Update the trace `events` field serialization into `response.trace: Vec<serde_json::Value>`.
       - Ensure that when `match_against_supersip_tables` returns `None` and there's no default, response.result is "abort" (existing behavior).
       - DO NOT touch `pub fn router()` signature.
    2. CREATE `tests/proxy_routing_match_types.rs` with 18 cases. Use:
       - `tests/common::build_app` for the HTTP harness.
       - `wiremock` for HTTP fixtures (HttpQuery cases).
       - Direct ActiveModel inserts to seed `supersip_routing_tables` rows (records as `serde_json::json!([...])`).
    3. Run `cargo test -p rustpbx --test proxy_routing_match_types` — all 18 GREEN.
    4. Run full regression: `cargo test -p rustpbx`.
  </action>
  <verify>
    <automated>cargo test -p rustpbx --test proxy_routing_match_types 2>&amp;1 | tail -3 | grep -qE "test result.*ok\\..*0 failed" &amp;&amp; grep -q "matched_record_id = trace" src/handler/api_v1/routing.rs || grep -q "matched_record_id: trace" src/handler/api_v1/routing.rs &amp;&amp; cargo test -p rustpbx --lib 2>&amp;1 | tail -3 | grep -q "0 failed"</automated>
  </verify>
  <done>
    - 18/18 IT-04 tests GREEN.
    - `/resolve` populates all three matched_* fields.
    - `cargo test -p rustpbx` full suite passes (with documented deltas if any Phase 1-5 matcher test had to be re-seeded for the supersip_ source).
    - `git diff src/handler/api_v1/mod.rs src/models/migration.rs` empty (file-ownership invariant preserved across all of Phase 6).
  </done>
  <acceptance_criteria>
    - `[ -f tests/proxy_routing_match_types.rs ]`
    - `grep -c "#\\[tokio::test\\]" tests/proxy_routing_match_types.rs` >= 18
    - `cargo test -p rustpbx --test proxy_routing_match_types` exits 0
    - `grep -q "matched_record_id" src/handler/api_v1/routing.rs` (populated, not just declared)
    - `git diff src/handler/api_v1/mod.rs` empty
    - `git diff src/models/migration.rs` empty
    - `git diff src/models/routing.rs` empty (legacy untouched per D-05)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INVITE → matcher | Untrusted SIP request (caller, destination, headers, src_ip) drives match evaluation |
| matcher → external HttpQuery URL | Operator-configured (and write-time-validated) URL receives a POST per INVITE |
| HttpQuery response → matcher | Untrusted JSON `{matched, target}` from operator's external service |
| matcher → DB (supersip_routing_tables) | Read-only fresh per INVITE (D-29) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Spoofing / SSRF (RUNTIME) | `eval_http_query` | mitigate | Even though 06-03 validates URL at write-time, runtime re-checks scheme + host against the same denylist on EVERY request before issuing it. Defends against (a) DNS-rebind to internal IP after passing write-time check, (b) DB tampering bypassing the API. Test `it04_http_query_runtime_ssrf_falls_through`. |
| T-06-04-02 | Tampering | Operator regex pattern complexity → ReDoS at match time | mitigate | 06-03 caps pattern length at 4096 chars. `eval_regex` uses Rust `regex` crate which is linear-time-guaranteed (no backtracking) — ReDoS is structurally impossible. Documented in module comment. |
| T-06-04-03 | Denial of Service | HttpQuery serial evaluation latency (D-17) | accept | Per D-17, HttpQuery records evaluated SERIALLY in position order. Operator-controlled latency budget. Recommend in 06-SUMMARY operators put cheap matches before HttpQuery. Parallel HttpQuery is v2.1. |
| T-06-04-04 | Denial of Service | HttpQuery timeout DoS | mitigate | Per-record timeout ≤ 5000ms (06-03 cap), default 2000ms. Failure → fall-through (NOT 503) per D-15. Test `it04_http_query_timeout_falls_through_to_default`. |
| T-06-04-05 | Denial of Service | next_table loop | mitigate | visited-set + depth cap 3 per D-25. Test `it04_next_table_chain_loop_detected_returns_abort`. |
| T-06-04-06 | Information Disclosure | RouteTrace events leak match type details | accept | Trace is internal — only emitted to `/api/v1/routing/resolve` (Bearer-auth) and structured logs. Operator already has DB read access. No new exposure. |
| T-06-04-07 | Tampering | Operator-supplied response body `{matched:true, target:{kind:trunk_group, name:"any"}}` | mitigate | Target name passed through; existing trunk_group/gateway dispatch validates name at dispatch time (Phase 2/3 contract). Rejected target → existing `RouteResult::Abort`/`Reject` path. |
| T-06-04-08 | Repudiation | matched_table/index/id traceability | mitigate | All three populated in resolve response + RouteTrace events; tests assert UUID v4 shape. Audit trail to 06-03's stable record_id. |
| T-06-04-09 | Spoofing | Caller-supplied src_ip in resolve dry-run | accept | `/resolve` is operator dry-run; src_ip is operator-supplied for testing match conditions. Production INVITE path uses Via-header peer IP (Phase 5 pattern). |
| T-06-04-10 | Elevation of Privilege | Legacy `rustpbx_routes` rows leaked into match | mitigate | D-06: legacy table NOT consulted when DB is available. Test `it04_legacy_rustpbx_routes_not_consulted_when_supersip_table_matches` proves silence. |
| T-06-04-11 | Tampering | Embedded JSON column size at read time | mitigate | 1000-record cap (06-03). Per-INVITE deserialize cost bounded. v2.1 candidate: cache parsed records keyed on table id + updated_at. |
| T-06-04-12 | Denial of Service | reqwest connection-pool exhaustion under sustained HttpQuery | mitigate | Single shared `reqwest::Client` (built once at server start) with default pool. Per-request timeout caps blocking. |
</threat_model>

<verification>
- `cargo test -p rustpbx --lib proxy::routing::match_types` ≥12/≥12 green.
- `cargo test -p rustpbx --lib proxy::routing::table_matcher` ≥10/≥10 green.
- `cargo test -p rustpbx --test proxy_routing_match_types` ≥18/≥18 green.
- `cargo test -p rustpbx --lib` baseline holds (Phase 5: 1154+; minor delta if matcher tests re-seeded — document in summary).
- `cargo test -p rustpbx --test api_v1_routing_resolve` (Phase 3 + 06-01) all green.
- `git diff src/models/migration.rs src/handler/api_v1/mod.rs src/models/routing.rs` empty.
- Manual: `curl -X POST -d '{"caller_number":"+14155551234","destination_number":"+14155551234"}' /api/v1/routing/resolve` returns response with `matched_record_id` non-null when a matching record exists.
</verification>

<success_criteria>
1. RTE-04: All 5 match types resolve correctly through `match_invite_with_trace`, covered by 18 IT-04 tests.
2. RTE-05: Default record returned when no rule matches; covered by `it04_default_record_returned_when_no_match`.
3. `/api/v1/routing/resolve` populates `matched_table`, `matched_record_index`, `matched_record_id` from supersip tables (D-30 wired).
4. Legacy `rustpbx_routes` no longer consulted by matcher when DB present (D-06); test `it04_legacy_rustpbx_routes_not_consulted_when_supersip_table_matches` proves.
5. `next_table` chain depth ≤ 3 with loop detection.
6. HttpQuery failures (timeout, 5xx, JSON, SSRF) all fall through; never 503.
7. File-ownership invariant: `src/handler/api_v1/mod.rs` and `src/models/migration.rs` UNCHANGED across all of Phase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/06-routing-tables-records-distribution/06-04-SUMMARY.md` recording:
- Test counts (unit + IT-04 + regression deltas if any).
- Operator-deploy callout: legacy `rustpbx_routes` rules must be re-created in `/api/v1/routing/tables` per D-06/D-07. This MUST be called out in `.planning/phases/06-routing-tables-records-distribution/06-SUMMARY.md` (created by checker / verify step) as a deploy-time manual action.
- Note any matcher test re-seeding deltas (Phase 1-5 tests that previously seeded via legacy `routes` arg and now seed via `supersip_routing_tables`).
</output>
