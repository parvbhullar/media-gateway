---
phase: 10-security-suite
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/proxy/auth.rs
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "Auth failures from (IP, realm) where Authorization header is present increment the brute-force counter"
    - "When brute-force threshold (10 failures/60s) is hit, write_block() is called via tokio::spawn fire-and-forget"
    - "First challenge (missing Authorization header) does NOT count as an auth failure"
    - "Hook wired into the auth-failure path in auth.rs lines 315-341 only"
  artifacts:
    - path: "src/proxy/auth.rs"
      provides: "auth failure hook calling security_state.record_auth_failure()"
      contains: "record_auth_failure"
  key_links:
    - from: "src/proxy/auth.rs auth failure path (lines 315-341)"
      to: "src/proxy/security_state.rs SecurityState::record_auth_failure()"
      via: "self.server.security_state.record_auth_failure(ip, realm)"
      pattern: "record_auth_failure"
    - from: "src/proxy/auth.rs threshold breach path"
      to: "src/proxy/security_state.rs SecurityState::write_block()"
      via: "tokio::spawn(async move { security.write_block(...).await.ok(); })"
      pattern: "write_block"
---

<objective>
Wire the brute-force auth-failure hook into auth.rs. When credentials are present but wrong, increment the per-(IP, realm) failure counter. On threshold breach, spawn a fire-and-forget task that writes the block row to DB immediately (D-09).

Purpose: Completes SEC-03 brute-force tracking. Runs in parallel with 10-02 (different file).
Output: auth.rs calls record_auth_failure() on every failed authentication attempt that has a credential header present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-security-suite/10-CONTEXT.md
@.planning/phases/10-security-suite/10-RESEARCH.md
@.planning/phases/10-security-suite/10-PATTERNS.md
@.planning/phases/10-security-suite/10-01-SUMMARY.md
</context>

<interfaces>
Read these files before making any edits:

From src/proxy/auth.rs (the target file):
- Read lines 1-50 for imports (need to understand what SipConnection import already exists)
- Read lines 290-360 for the full auth failure path including realm extraction and tx.reply_with pattern
- The hook window (RESEARCH.md): lines 315-341 cover the path where authenticate_request returns Ok(None) — credentials present but wrong
- D-10: Hook fires only when Authorization OR Proxy-Authorization header IS present (not first challenge)

From src/proxy/security_state.rs (Wave 1 output):
- record_auth_failure(ip: IpAddr, realm: String) -> bool: true when threshold hit
- write_block(db: &DatabaseConnection, ip: IpAddr, realm: &str, reason: &str) -> anyhow::Result<()>

Realm extraction pattern already in auth.rs:
  let request_host = tx.original.uri().host().to_string();
  let realm = self.server.proxy_config.select_realm(request_host.as_str());

IP extraction MUST match acl.rs pattern (RISK-01 from RESEARCH.md — same Via-based extraction):
  let via = tx.original.via_header()?;
  let (_, target) = SipConnection::parse_target_from_via(via)
      .map_err(|e| anyhow::anyhow!("{}", e))?;
  let from_addr: IpAddr = target.host.try_into()?;
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth failure brute-force hook in auth.rs (SEC-03)</name>
  <read_first>
    - src/proxy/auth.rs lines 1-50 (imports and struct) and lines 290-370 (auth failure path)
    - src/proxy/security_state.rs (record_auth_failure and write_block signatures from Wave 1)
    - src/proxy/acl.rs lines 285-295 (IP extraction pattern to copy verbatim)
  </read_first>
  <files>src/proxy/auth.rs</files>
  <behavior>
    - Test 1: Auth failure with Authorization header present increments brute-force counter
    - Test 2: Auth failure WITHOUT Authorization header (first challenge) does NOT increment counter
    - Test 3: After 10 failures from same (ip, realm), write_block is called (threshold breach returns true)
  </behavior>
  <action>
Read src/proxy/auth.rs lines 290-370 fully before editing. Find the exact code path where:
- authenticate_request returns Ok(None) (credentials present but wrong), OR
- the auth challenge reply is sent with 401 Unauthorized or 407 Proxy Auth Required

The insertion goes BEFORE tx.reply_with(...) / the Abort return in that failure block.

Insertion (per PATTERNS.md lines 528-565 — D-10 semantics):

Add a guard checking for credential header presence:
  if tx.original.header_by_name("Authorization").is_some()
      || tx.original.header_by_name("Proxy-Authorization").is_some()

Inside the guard, extract IP using the acl.rs Via pattern (copy verbatim from acl.rs lines 288-293 — see RESEARCH.md lines 153-159). Then:
  let realm = ... (already computed in auth.rs context);
  if let Ok(via) = tx.original.via_header() {
      if let Ok((_, target)) = SipConnection::parse_target_from_via(via) {
          if let Ok(ip) = <IpAddr>::try_from(target.host) {
              let threshold_hit = self.server.security_state
                  .record_auth_failure(ip, realm.clone());
              if threshold_hit {
                  let db = self.server.database.clone();
                  let security = self.server.security_state.clone();
                  let realm_owned = realm.clone();
                  tokio::spawn(async move {
                      if let Some(db) = db {
                          security.write_block(&db, ip, &realm_owned, "brute_force")
                              .await
                              .ok();
                      }
                  });
              }
          }
      }
  }

The insertion must be ADDITIVE only — do not restructure or rename any existing variables. The block goes inside the existing failure path without changing control flow. The tx.reply_with() and ProxyAction::Abort remain unchanged after the insertion.

If the auth.rs failure path spans multiple match arms (REGISTER vs INVITE), insert the hook into BOTH arms (or extract a shared helper closure). Study the actual code structure before deciding.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice/media-gateway && cargo build -p rustpbx 2>&1 | grep -E "^error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "record_auth_failure" src/proxy/auth.rs shows the hook call in the failure path
    - grep -n "write_block" src/proxy/auth.rs shows the fire-and-forget tokio::spawn block
    - grep -n "Authorization" src/proxy/auth.rs near the hook shows the header-presence guard
    - Diff of auth.rs shows ONLY additive lines (no existing lines deleted or modified)
    - cargo build -p rustpbx exits 0 with no errors
  </acceptance_criteria>
  <done>auth.rs calls record_auth_failure() on credential failures; write_block spawned on threshold breach; first-challenge path unchanged; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SIP auth failure -> brute-force counter | Attacker-controlled IP and realm flow into DashMap keyed (ip, realm) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | IP extraction in auth failure hook | mitigate | Same Via-based extraction as acl.rs (RISK-01); consistent with SecurityModule hot-path |
| T-10-03-02 | Denial of Service | write_block tokio::spawn under high auth failure rate | mitigate | tokio::spawn is fire-and-forget; failure to write block logs warn but does not block auth processing; threshold is 10 failures so spawn rate is bounded |
| T-10-03-03 | Tampering | block row write bypasses cache refresh | mitigate | write_block calls load_block_cache_from_db after INSERT to refresh in-memory cache; subsequent SecurityModule checks see the new block |
| T-10-03-04 | Elevation of Privilege | brute-force threshold counting first challenges | mitigate | Guard checks Authorization/Proxy-Authorization header presence before counting; first-challenge (missing header) is not counted per D-10 |
</threat_model>

<verification>
Build check:
  cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice/media-gateway && cargo build -p rustpbx 2>&1 | grep -E "^error"
  Expect: no output

Hook presence:
  grep -n "record_auth_failure" src/proxy/auth.rs
  Expect: at least one line in the auth failure path

Write-block spawn:
  grep -n "write_block" src/proxy/auth.rs
  Expect: present in tokio::spawn block

Header guard:
  grep -n "Authorization" src/proxy/auth.rs | grep -i "header_by_name\|is_some"
  Expect: guard check present near the hook insertion

Additive-only check:
  git diff src/proxy/auth.rs | grep "^-" | grep -v "^---" | wc -l
  Expect: 0 (no deleted lines — pure insertion)
</verification>

<success_criteria>
- cargo build -p rustpbx exits 0 with no new errors
- grep finds record_auth_failure in auth.rs auth failure code path
- grep finds write_block in auth.rs inside a tokio::spawn
- Authorization header presence guard is visible in diff
- git diff shows 0 deleted lines in auth.rs (purely additive)
</success_criteria>

<output>
After completion, create .planning/phases/10-security-suite/10-03-SUMMARY.md covering:
- Exact file:line of insertion in auth.rs
- Code diff summary (lines added)
- Which auth paths (REGISTER, INVITE, or both) were hooked
- Cargo build status
</output>
