---
phase: 10-security-suite
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - src/proxy/proxy_call/sip_session.rs
  - tests/api_v1_security.rs
  - scripts/run_tests.sh
autonomous: true
requirements: [SEC-06, SEC-01, SEC-02, SEC-03, SEC-04, SEC-05]

must_haves:
  truths:
    - "topology_hiding=true in ProxyConfig causes Via and Record-Route header retention to strip from outbound headers in accept_call"
    - "topology_hiding=false (default) leaves outbound headers untouched"
    - "Integration tests: 401 without auth, 200 GET firewall, 200 PATCH firewall with valid rule, 200 GET flood-tracker, 200 GET blocks, 204 DELETE blocks/{ip}, 200 GET auth-failures"
    - "Integration test confirms PATCH firewall with invalid CIDR returns 400"
    - "Test file registered in scripts/run_tests.sh"
  artifacts:
    - path: "src/proxy/proxy_call/sip_session.rs"
      provides: "topology hiding insertion in accept_call (additive, <= 10 lines)"
      contains: "topology_hiding"
    - path: "tests/api_v1_security.rs"
      provides: "IT-01 integration tests for security endpoints"
      exports: ["all test functions"]
  key_links:
    - from: "src/proxy/proxy_call/sip_session.rs accept_call()"
      to: "src/config.rs ProxyConfig::topology_hiding"
      via: "self.server.proxy_config.topology_hiding.unwrap_or(false)"
      pattern: "topology_hiding"
---

<objective>
Wave 3: Topology hiding insertion in sip_session.rs outbound path (SEC-06), plus the IT-01 integration test suite for all security endpoints.

Purpose: SEC-06 (topology hiding) + IT-01 (integration tests for SEC-01..SEC-05 REST surface).
Output: topology_hiding config flag strips Via/Record-Route in outbound responses when enabled; full test suite exercises all 5 security endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-security-suite/10-CONTEXT.md
@.planning/phases/10-security-suite/10-RESEARCH.md
@.planning/phases/10-security-suite/10-PATTERNS.md
@.planning/phases/10-security-suite/10-01-SUMMARY.md
@.planning/phases/10-security-suite/10-02-SUMMARY.md
@.planning/phases/10-security-suite/10-03-SUMMARY.md
</context>

<interfaces>
Read these files before writing any code.

From src/proxy/proxy_call/sip_session.rs (topology hiding insertion point):
- Read accept_call method fully — specifically find the headers Vec assembly and the server_dialog.accept() call
- PATTERNS.md lines 499-520 shows the exact insertion point: just before server_dialog.accept()
- The insertion is <= 10 additive lines (D-18)
- Caveat from RESEARCH.md (RISK-04): B2BUA accept() may already exclude proxy Via hops; strip is defensive
- self.server.proxy_config is accessible; field is topology_hiding: Option<bool>

From tests/api_v1_translations.rs or tests/api_v1_trunk_acl.rs (IT-01 test pattern):
- Test helper pattern: use common::build_test_state() or equivalent app setup
- Auth pattern: tests include both 401-without-auth and 200-with-valid-bearer cases
- Study one existing test file to understand the exact test harness shape

From src/handler/api_v1/security.rs (Wave 2 output):
- Router paths: /security/firewall, /security/flood-tracker, /security/blocks, /security/blocks/:ip, /security/auth-failures
- HTTP methods: GET firewall, PATCH firewall, GET flood-tracker, GET blocks, DELETE blocks/:ip, GET auth-failures

From scripts/run_tests.sh:
- Understand how to register a new test module (study how proxy_manipulations_pipeline was added in Phase 9)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Topology hiding in sip_session.rs (SEC-06)</name>
  <read_first>
    - src/proxy/proxy_call/sip_session.rs accept_call method (read the full outbound 200 OK path)
    - src/proxy/nat.rs (understand what Via handling already exists; coordinate not duplicate)
    - src/config.rs (confirm topology_hiding field added in Wave 1)
    - .planning/phases/10-security-suite/10-RESEARCH.md lines 215-238 (B2BUA caveat and exact code)
  </read_first>
  <files>src/proxy/proxy_call/sip_session.rs</files>
  <action>
Find the accept_call method in sip_session.rs. Locate the block that:
1. Assembles a Vec of extra headers (e.g., ContentType("application/sdp"), timer headers)
2. Calls self.server_dialog.accept(Some(headers), Some(sdp_bytes))

Insert BEFORE the accept() call, AFTER headers Vec is fully assembled (match PATTERNS.md lines 509-519):

  // Topology hiding (Phase 10 D-16/D-18): strip Via and Record-Route from
  // outbound 200 OK extra-headers when topology_hiding is enabled.
  // B2BUA note: rsipstack ServerInviteDialog::accept() constructs the
  // response fresh; these extra-headers may not include proxy-added Via
  // hops. The retain calls are defensive (RISK-04 from RESEARCH.md).
  if self.server.proxy_config.topology_hiding.unwrap_or(false) {
      headers.retain(|h| !matches!(h, rsipstack::sip::Header::Via(_)));
      headers.retain(|h| !matches!(h, rsipstack::sip::Header::RecordRoute(_)));
  }

The exact rsipstack::sip::Header enum variants must be verified by reading the actual headers Vec type before editing. If the variant names differ (e.g., Header::Via vs Header::ViaHeader), use the correct names from the rsipstack types in scope at this call site.

This MUST be a purely additive change. Do not alter any existing lines.
Budget: <= 10 new lines (D-18 constraint).
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice/media-gateway && cargo build -p rustpbx 2>&1 | grep -E "^error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "topology_hiding" src/proxy/proxy_call/sip_session.rs shows the new insertion
    - git diff src/proxy/proxy_call/sip_session.rs | grep "^-" | grep -v "^---" | wc -l outputs 0 (no deleted lines)
    - git diff src/proxy/proxy_call/sip_session.rs | grep "^+" | grep -v "^+++" | wc -l outputs <= 10
    - cargo build -p rustpbx exits 0 with no errors
  </acceptance_criteria>
  <done>Topology hiding insertion compiles; <= 10 new lines; no existing lines deleted; cargo build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IT-01 integration tests for security endpoints</name>
  <read_first>
    - tests/api_v1_trunk_acl.rs (IT-01 test pattern — 401-without-auth, happy path, bad input structure)
    - tests/api_v1_translations.rs (alternative test pattern reference)
    - tests/common/ directory (build_test_state helper or equivalent)
    - scripts/run_tests.sh (module registration pattern from Phase 9 additions)
    - src/handler/api_v1/security.rs (exact route paths and response shapes from Wave 2)
  </read_first>
  <files>tests/api_v1_security.rs, scripts/run_tests.sh</files>
  <behavior>
    - Test 1: GET /api/v1/security/firewall without auth returns 401
    - Test 2: GET /api/v1/security/firewall with valid auth returns 200 + empty array on clean DB
    - Test 3: PATCH /api/v1/security/firewall with valid rule {"rules":[{"position":1,"action":"deny","cidr":"10.0.0.0/8","description":null}]} returns 200 with the new rule
    - Test 4: PATCH /api/v1/security/firewall with invalid cidr returns 400
    - Test 5: GET /api/v1/security/flood-tracker with valid auth returns 200 + {"data":[]}
    - Test 6: GET /api/v1/security/blocks with valid auth returns 200 + {"data":[]}
    - Test 7: DELETE /api/v1/security/blocks/1.2.3.4 on non-existent block returns 404
    - Test 8: GET /api/v1/security/auth-failures with valid auth returns 200 + {"data":[]}
  </behavior>
  <action>
Create tests/api_v1_security.rs following the exact pattern from tests/api_v1_trunk_acl.rs:

1. Read tests/api_v1_trunk_acl.rs fully to understand:
   - How to construct the test app (build_test_state, axum test client setup)
   - How to send authenticated requests (Bearer token header)
   - How to send unauthenticated requests (401 test)
   - How to parse JSON responses and assert fields

2. Write test functions matching the 8 behavior cases above. Follow the exact naming convention from api_v1_trunk_acl.rs tests.

3. For the PATCH firewall tests, use serde_json::json! macro to construct request bodies.

4. For the 400 invalid CIDR test, pass a rule with cidr = "not-a-cidr" and assert StatusCode::BAD_REQUEST.

5. Register in scripts/run_tests.sh — find the section where api_v1_trunk_acl or proxy_manipulations_pipeline was added in Phase 9; add a parallel entry for api_v1_security.

The test suite MUST cover IT-01 requirements: 401-without-auth, happy-path, 404-missing, 400-bad-input.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice/media-gateway && cargo test -p rustpbx --test api_v1_security 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "async fn " tests/api_v1_security.rs shows at least 8 test functions
    - grep -n "StatusCode::UNAUTHORIZED\|401" tests/api_v1_security.rs shows auth guard test
    - grep -n "StatusCode::BAD_REQUEST\|400" tests/api_v1_security.rs shows validation test
    - grep -n "api_v1_security" scripts/run_tests.sh shows module registration
    - cargo test -p rustpbx --test api_v1_security exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 8 test cases pass; module registered in run_tests.sh; cargo test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| topology_hiding config flag -> sip_session.rs header strip | Config value controls outbound SIP header stripping |
| Test suite -> security endpoints | Tests assert auth enforcement on all security routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | topology_hiding Via/Record-Route strip | accept | B2BUA already constructs fresh responses; strip is defensive no-op in most cases (RISK-04). No new exposure introduced. |
| T-10-04-02 | Elevation of Privilege | Security endpoints auth bypass | mitigate | IT-01 test case "401 without auth" asserts all 5 security endpoints require Bearer token; enforced by existing middleware in mod.rs |
| T-10-04-03 | Tampering | topology_hiding disabled by default | accept | Option<bool> with unwrap_or(false) means topology hiding is opt-in; existing behavior preserved when flag absent from config |
</threat_model>

<verification>
Build check:
  cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice/media-gateway && cargo build -p rustpbx 2>&1 | grep -E "^error"
  Expect: no output

Topology hiding additive-only:
  git diff src/proxy/proxy_call/sip_session.rs | grep "^-" | grep -v "^---" | wc -l
  Expect: 0

Topology hiding line budget:
  git diff src/proxy/proxy_call/sip_session.rs | grep "^+" | grep -v "^+++" | wc -l
  Expect: <= 10

Integration test suite passes:
  cargo test -p rustpbx --test api_v1_security 2>&1 | tail -5
  Expect: "test result: ok. N passed; 0 failed"

Test module registered:
  grep "api_v1_security" scripts/run_tests.sh
  Expect: entry present
</verification>

<success_criteria>
- cargo build -p rustpbx exits 0 with no new errors
- grep finds topology_hiding in sip_session.rs at the accept_call outbound path
- git diff shows 0 deleted lines and <= 10 added lines in sip_session.rs
- cargo test -p rustpbx --test api_v1_security passes all 8+ tests
- All 6 requirements (SEC-01..SEC-06) are satisfied:
  - SEC-01: GET/PATCH /security/firewall functional
  - SEC-02: SecurityModule flood check wired (10-02)
  - SEC-03: Brute-force hook in auth.rs (10-03) + blocks written to DB
  - SEC-04: GET/DELETE /security/blocks functional
  - SEC-05: GET /security/auth-failures functional
  - SEC-06: topology_hiding insertion in sip_session.rs accept_call
</success_criteria>

<output>
After completion, create .planning/phases/10-security-suite/10-04-SUMMARY.md covering:
- Topology hiding exact file:line insertion in sip_session.rs
- Line count added (must be <= 10)
- Test suite results (N tests passed)
- Full Phase 10 requirements satisfaction table (SEC-01..SEC-06 with file:line citations)
- Phase 10 closure statement
</output>
