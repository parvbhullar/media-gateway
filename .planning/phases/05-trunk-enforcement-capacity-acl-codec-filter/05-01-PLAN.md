---
phase: 05-trunk-enforcement-capacity-acl-codec-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/trunk_capacity.rs
  - src/models/trunk_acl_entries.rs
  - src/models/drop_acl_column.rs
  - src/models/mod.rs
  - src/models/migration.rs
  - src/models/trunk_group.rs
  - src/handler/api_v1/trunk_capacity.rs
  - src/handler/api_v1/trunk_acl.rs
  - src/handler/api_v1/mod.rs
autonomous: true
requirements:
  - TSUB-04
  - TSUB-05
must_haves:
  truths:
    - "supersip_trunk_capacity table exists after Migrator::up() on a fresh DB"
    - "supersip_trunk_acl_entries table exists after Migrator::up() on a fresh DB"
    - "rustpbx_trunk_groups.acl column does NOT exist after Migrator::up()"
    - "trunk_group::Model has no acl field; cargo check -p rustpbx --lib is clean"
    - "FK CASCADE deletes capacity + acl_entries rows when their trunk_group is deleted"
    - "src/handler/api_v1/trunk_capacity.rs and src/handler/api_v1/trunk_acl.rs exist as stub modules with empty router() functions, registered + merged in mod.rs (handlers filled in by Plans 05-02 and 05-03)"
  artifacts:
    - path: "src/models/trunk_capacity.rs"
      provides: "supersip_trunk_capacity entity + migration (TSUB-04 storage)"
      contains: "supersip_trunk_capacity"
    - path: "src/models/trunk_acl_entries.rs"
      provides: "supersip_trunk_acl_entries entity + migration (TSUB-05 storage)"
      contains: "supersip_trunk_acl_entries"
    - path: "src/models/drop_acl_column.rs"
      provides: "Forward-only migration that drops rustpbx_trunk_groups.acl"
      contains: "drop_column"
    - path: "src/models/migration.rs"
      provides: "Three migrations registered in FK-safe order"
      contains: "trunk_capacity::Migration"
    - path: "src/models/trunk_group.rs"
      provides: "Model without acl field; CREATE TABLE without Acl column"
      contains: "media_config: Option<Json>"
    - path: "src/handler/api_v1/trunk_capacity.rs"
      provides: "Stub handler module — empty router(); Plan 05-02 fills in handlers"
      exports: ["router"]
    - path: "src/handler/api_v1/trunk_acl.rs"
      provides: "Stub handler module — empty router(); Plan 05-03 fills in handlers"
      exports: ["router"]
    - path: "src/handler/api_v1/mod.rs"
      provides: "Both stub modules declared and merged into protected /api/v1 router"
      contains: "trunk_capacity::router\\|trunk_acl::router"
  key_links:
    - from: "src/models/trunk_capacity.rs"
      to: "rustpbx_trunk_groups.id"
      via: "FK fk_supersip_trunk_capacity_group_id ON DELETE CASCADE"
      pattern: "fk_supersip_trunk_capacity_group_id"
    - from: "src/models/trunk_acl_entries.rs"
      to: "rustpbx_trunk_groups.id"
      via: "FK fk_supersip_trunk_acl_entries_group_id ON DELETE CASCADE"
      pattern: "fk_supersip_trunk_acl_entries_group_id"
    - from: "src/models/migration.rs"
      to: "trunk_capacity / trunk_acl_entries / drop_acl_column"
      via: "Migrator::migrations() ordered append"
      pattern: "trunk_capacity::Migration"
    - from: "src/handler/api_v1/mod.rs"
      to: "trunk_capacity::router / trunk_acl::router"
      via: ".merge(...)" 
      pattern: ".merge(trunk_capacity::router())\\|.merge(trunk_acl::router())"
---

<objective>
Land schema for Phase 5 enforcement: two new sub-resource tables (`supersip_trunk_capacity`,
`supersip_trunk_acl_entries`) and a forward-only drop of the legacy `rustpbx_trunk_groups.acl`
JSON column. Schema only — plus stub handler modules so the protected `/api/v1` router merge
graph is owned exclusively by Wave 1. Wave 2 plans (05-02, 05-03) ONLY fill in the stub
files' handler bodies — they do NOT touch `mod.rs` (avoids parallel mod.rs edit conflicts).
Wave 3 plan (05-04) wires proxy enforcement.

Implements:
- D-01 (capacity sub-resource pattern, UNIQUE FK)
- D-10 (ACL promoted from JSON column to multi-row table; mirrors Phase 3 pattern)
- D-11 (DROP rustpbx_trunk_groups.acl in same migration set, runs LAST)
- Migration registration order from `<decisions>` block

Purpose: unblock Wave 2 CRUD handlers and Wave 3 enforcement by locking storage shape
AND the router merge graph first. Stub creation in Wave 1 means Wave 2 plans modify
disjoint files (`trunk_capacity.rs` vs `trunk_acl.rs`) and can run truly in parallel.
Output: 3 migrations registered, 2 entities exported, trunk_group::Model without acl,
2 stub handler modules registered + merged in api_v1/mod.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-CONTEXT.md
@.planning/phases/03-trunk-sub-resources-l1-routing-resolve/03-01-SUMMARY.md
@src/models/trunk_credentials.rs
@src/models/trunk_origination_uris.rs
@src/models/trunk_group.rs
@src/models/migration.rs
@src/models/drop_credentials_column.rs
@src/handler/api_v1/mod.rs
@src/handler/api_v1/trunk_credentials.rs

<interfaces>
Phase 3 sub-resource pattern, copied verbatim:

From src/models/trunk_credentials.rs (analog for trunk_capacity but with UNIQUE FK):
```rust
#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "supersip_trunk_credentials")]
pub struct Model { id: i64, trunk_group_id: i64, ... }

// FK: belongs_to trunk_group, on_delete = Cascade, on_update = Cascade
// Migration: create_table with FK::create().on_delete(ForeignKeyAction::Cascade)
```

From src/models/trunk_origination_uris.rs (analog for trunk_acl_entries — multi-row + position):
```rust
pub struct Model {
    pub id: i64,
    pub trunk_group_id: i64,
    pub uri: String,
    pub position: i32,
    pub created_at: DateTimeUtc,
}
// UNIQUE (trunk_group_id, uri); non-unique (trunk_group_id, position)
```

From src/models/trunk_group.rs:75 (existing acl column to remove):
```rust
pub acl: Option<Json>,    // <-- REMOVE from Model
.col(json_null(Column::Acl))    // <-- REMOVE from Migration::up CREATE TABLE
```

From src/models/migration.rs (registration order — append AFTER drop_credentials_column):
```rust
// Phase 5 Plan 05-01 — TSUB-04 + TSUB-05 schema. FK-safe order:
//   1. Create capacity (UNIQUE FK to rustpbx_trunk_groups.id)
//   2. Create acl_entries (FK to rustpbx_trunk_groups.id, UNIQUE rule)
//   3. Drop legacy acl column LAST so any in-flight reads succeed during deploy
Box::new(super::trunk_capacity::Migration),
Box::new(super::trunk_acl_entries::Migration),
Box::new(super::drop_acl_column::Migration),
```

Stub handler module shape (Task 5 output — Plans 05-02/05-03 will replace bodies):
```rust
// src/handler/api_v1/trunk_capacity.rs (stub)
use axum::Router;
use crate::AppState;   // adjust path to match other api_v1 modules

pub fn router() -> Router<AppState> {
    Router::new()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create supersip_trunk_capacity entity + migration</name>
  <files>src/models/trunk_capacity.rs, src/models/mod.rs</files>
  <read_first>
    - src/models/trunk_credentials.rs (pattern to copy)
    - src/models/mod.rs (where to add `pub mod trunk_capacity;`)
    - .planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-CONTEXT.md (D-01 — schema shape)
  </read_first>
  <action>
    Create new file `src/models/trunk_capacity.rs` modeled on `trunk_credentials.rs`.

    Entity fields (D-01):
    - `id: i64` PK auto_increment
    - `trunk_group_id: i64` (UNIQUE — at most one capacity row per trunk group)
    - `max_calls: Option<i32>` (null = unlimited per D-04)
    - `max_cps: Option<i32>` (null = unlimited per D-04)
    - `created_at: DateTimeUtc` default current_timestamp
    - `updated_at: DateTimeUtc` default current_timestamp

    Table name: `supersip_trunk_capacity`. Use `#[sea_orm(table_name = "supersip_trunk_capacity")]`.

    Relation to `super::trunk_group::Entity`:
    `belongs_to`, `on_update = "Cascade"`, `on_delete = "Cascade"`.

    Migration::up():
    - `create_table` IF NOT EXISTS with the columns above. Use `integer_null` (or `ColumnDef::new(...).integer().null()`) for max_calls and max_cps; do NOT make them NOT NULL.
    - FK named `fk_supersip_trunk_capacity_group_id` from `(Entity, Column::TrunkGroupId)` to `(super::trunk_group::Entity, super::trunk_group::Column::Id)` with `ForeignKeyAction::Cascade` on both delete and update.
    - Create UNIQUE index `idx_supersip_trunk_capacity_group_id` on `Column::TrunkGroupId` (D-01: 1 row per trunk_group max).

    Migration::down(): drop_table.

    Add `pub mod trunk_capacity;` to `src/models/mod.rs` (alphabetical order with the other Phase 3 sub-resource mods near `pub mod trunk_credentials;`).

    DO NOT register in `migration.rs` yet — Task 4 does that atomically with all three.
  </action>
  <verify>
    <automated>cargo check -p rustpbx --lib 2>&amp;1 | grep -E "error|^warning" | head -20 ; test -f src/models/trunk_capacity.rs &amp;&amp; grep -q "supersip_trunk_capacity" src/models/trunk_capacity.rs &amp;&amp; grep -q "fk_supersip_trunk_capacity_group_id" src/models/trunk_capacity.rs &amp;&amp; grep -q "pub mod trunk_capacity" src/models/mod.rs &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - File `src/models/trunk_capacity.rs` exists, mirrors trunk_credentials.rs structure
    - Table name string `supersip_trunk_capacity` present
    - UNIQUE index on trunk_group_id (not on trunk_group_id+something_else — bare column UNIQUE)
    - FK CASCADE configured both delete and update
    - `pub mod trunk_capacity;` added to src/models/mod.rs
    - cargo check -p rustpbx --lib clean
  </done>
  <acceptance_criteria>
    - `grep -c '#\\[sea_orm(table_name = "supersip_trunk_capacity")\\]' src/models/trunk_capacity.rs` == 1
    - `grep -c 'pub trunk_group_id: i64' src/models/trunk_capacity.rs` == 1
    - `grep -c 'pub max_calls: Option&lt;i32&gt;' src/models/trunk_capacity.rs` == 1
    - `grep -c 'pub max_cps: Option&lt;i32&gt;' src/models/trunk_capacity.rs` == 1
    - `grep -c 'fk_supersip_trunk_capacity_group_id' src/models/trunk_capacity.rs` == 1
    - `grep -c 'idx_supersip_trunk_capacity_group_id' src/models/trunk_capacity.rs` == 1
    - `grep -c '\\.unique()' src/models/trunk_capacity.rs` &gt;= 1
    - `grep -c 'ForeignKeyAction::Cascade' src/models/trunk_capacity.rs` &gt;= 2
    - `grep -c 'pub mod trunk_capacity' src/models/mod.rs` == 1
    - `cargo check -p rustpbx --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create supersip_trunk_acl_entries entity + migration</name>
  <files>src/models/trunk_acl_entries.rs, src/models/mod.rs</files>
  <read_first>
    - src/models/trunk_origination_uris.rs (closest analog: multi-row + position + UNIQUE on (parent, value))
    - .planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-CONTEXT.md (D-10, D-12, D-13)
    - src/models/mod.rs
  </read_first>
  <action>
    Create new file `src/models/trunk_acl_entries.rs` modeled on `trunk_origination_uris.rs`.

    Entity fields (D-10):
    - `id: i64` PK auto_increment
    - `trunk_group_id: i64` (FK CASCADE — multi-row per trunk_group)
    - `rule: String` char_len 255 (D-13 wire format: `^(allow|deny) (all|<CIDR>|<IP>)$`; validation lives in handler at Plan 05-03, NOT in the model)
    - `position: i32` default 0 (auto-assigned by handler in 05-03, mirrors origination_uris pattern)
    - `created_at: DateTimeUtc` default current_timestamp

    Table name: `supersip_trunk_acl_entries`.

    Relation to `super::trunk_group::Entity`: belongs_to, both Cascade.

    Migration::up():
    - create_table IF NOT EXISTS with the columns above
    - FK named `fk_supersip_trunk_acl_entries_group_id`, both Cascade
    - UNIQUE index `idx_supersip_trunk_acl_entries_group_rule` on `(Column::TrunkGroupId, Column::Rule)` (D-10)
    - Non-unique index `idx_supersip_trunk_acl_entries_group_pos` on `(Column::TrunkGroupId, Column::Position)` (used by handler ORDER BY position in 05-03)

    Migration::down(): drop_table.

    Add `pub mod trunk_acl_entries;` to `src/models/mod.rs` next to trunk_capacity (alphabetical).
  </action>
  <verify>
    <automated>cargo check -p rustpbx --lib 2>&amp;1 | grep -E "^error" | head ; test -f src/models/trunk_acl_entries.rs &amp;&amp; grep -q "supersip_trunk_acl_entries" src/models/trunk_acl_entries.rs &amp;&amp; grep -q "fk_supersip_trunk_acl_entries_group_id" src/models/trunk_acl_entries.rs &amp;&amp; grep -q "pub mod trunk_acl_entries" src/models/mod.rs &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - File `src/models/trunk_acl_entries.rs` exists
    - UNIQUE index on (trunk_group_id, rule) — 2-column composite
    - Non-unique index on (trunk_group_id, position)
    - FK CASCADE both delete and update
    - `pub mod trunk_acl_entries;` registered
    - cargo check clean
  </done>
  <acceptance_criteria>
    - `grep -c '#\\[sea_orm(table_name = "supersip_trunk_acl_entries")\\]' src/models/trunk_acl_entries.rs` == 1
    - `grep -c 'pub rule: String' src/models/trunk_acl_entries.rs` == 1
    - `grep -c 'pub position: i32' src/models/trunk_acl_entries.rs` == 1
    - `grep -c 'idx_supersip_trunk_acl_entries_group_rule' src/models/trunk_acl_entries.rs` == 1
    - `grep -c 'idx_supersip_trunk_acl_entries_group_pos' src/models/trunk_acl_entries.rs` == 1
    - `grep -c 'fk_supersip_trunk_acl_entries_group_id' src/models/trunk_acl_entries.rs` == 1
    - `grep -c 'ForeignKeyAction::Cascade' src/models/trunk_acl_entries.rs` &gt;= 2
    - `grep -c 'pub mod trunk_acl_entries' src/models/mod.rs` == 1
    - `cargo check -p rustpbx --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create drop_acl_column migration + remove acl from trunk_group::Model and CREATE TABLE</name>
  <files>src/models/drop_acl_column.rs, src/models/mod.rs, src/models/trunk_group.rs</files>
  <read_first>
    - src/models/drop_credentials_column.rs (pattern — has_column-guarded forward-only drop)
    - src/models/trunk_group.rs (find `pub acl: Option&lt;Json&gt;` field and `.col(json_null(Column::Acl))` line)
    - .planning/phases/03-trunk-sub-resources-l1-routing-resolve/03-01-SUMMARY.md ("Auto-fixed Issues" — explains why Model field AND CREATE TABLE entry must both go)
  </read_first>
  <action>
    Create new file `src/models/drop_acl_column.rs` modeled on `drop_credentials_column.rs`.

    Migration::up():
    - Use `manager.has_column("rustpbx_trunk_groups", "acl").await?` guard for idempotency
    - If present, `Table::alter().table(trunk_group::Entity).drop_column(trunk_group::Column::Acl).to_owned()` — wait, see note below
    - Use the same pattern that drop_credentials_column.rs uses (after Task 4 below removes the Column variant, you can reference the column by `Alias::new("acl")` instead of the enum variant; that's exactly what drop_credentials_column.rs does)

    Migration::down(): no-op (forward-only per Phase 3 D-02 precedent and Phase 5 D-11).

    Then modify `src/models/trunk_group.rs`:
    - Remove `pub acl: Option&lt;Json&gt;,` from the Model struct (line ~75)
    - Remove the `.col(json_null(Column::Acl))` line from `Migration::up()` CREATE TABLE block

    Removing the Model field eliminates the `Column::Acl` enum variant (SeaORM derives Column from Model). That is why the new drop migration must use `Alias::new("acl")` (string-based) not `Column::Acl` — matches the precedent set by drop_credentials_column.rs after credentials field removal.

    Add `pub mod drop_acl_column;` to `src/models/mod.rs`.
  </action>
  <verify>
    <automated>cargo check -p rustpbx --lib 2>&amp;1 | grep "^error" | head ; test -f src/models/drop_acl_column.rs &amp;&amp; grep -q 'has_column' src/models/drop_acl_column.rs &amp;&amp; grep -q '"acl"' src/models/drop_acl_column.rs &amp;&amp; ! grep -q 'pub acl: Option&lt;Json&gt;' src/models/trunk_group.rs &amp;&amp; ! grep -q 'json_null(Column::Acl)' src/models/trunk_group.rs &amp;&amp; grep -q 'pub mod drop_acl_column' src/models/mod.rs &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - File `src/models/drop_acl_column.rs` exists with `has_column` guard
    - `acl` field removed from `trunk_group::Model`
    - `json_null(Column::Acl)` line removed from `trunk_group::Migration::up()` CREATE TABLE
    - `pub mod drop_acl_column;` registered
    - cargo check -p rustpbx --lib is clean (no remaining `Column::Acl` references)
  </done>
  <acceptance_criteria>
    - `grep -c 'has_column' src/models/drop_acl_column.rs` &gt;= 1
    - `grep -c 'rustpbx_trunk_groups' src/models/drop_acl_column.rs` &gt;= 1
    - `grep -c 'Alias::new("acl")\\|"acl"' src/models/drop_acl_column.rs` &gt;= 1
    - `grep -c 'pub acl: Option&lt;Json&gt;' src/models/trunk_group.rs` == 0
    - `grep -c 'json_null(Column::Acl)' src/models/trunk_group.rs` == 0
    - `grep -c 'Column::Acl' src/ -r` reports 0 matches in src/ tree
    - `grep -c 'pub mod drop_acl_column' src/models/mod.rs` == 1
    - `cargo check -p rustpbx --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Register all three migrations in FK-safe order</name>
  <files>src/models/migration.rs</files>
  <read_first>
    - src/models/migration.rs (current state — Phase 3 block ends at drop_credentials_column line ~52)
    - .planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-CONTEXT.md (Migration registration order block)
  </read_first>
  <action>
    Append to the `migrations()` Vec in `src/models/migration.rs`, immediately after
    `Box::new(super::drop_credentials_column::Migration),`:

    ```rust
            // Phase 5 Plan 05-01 — TSUB-04 (capacity) + TSUB-05 (ACL).
            // Order is load-bearing (FK + safe-drop):
            //   1. Create supersip_trunk_capacity (UNIQUE FK to rustpbx_trunk_groups.id)
            //   2. Create supersip_trunk_acl_entries (FK CASCADE)
            //   3. Drop legacy rustpbx_trunk_groups.acl LAST so any in-flight reads
            //      during deploy succeed; mirrors Phase 3 D-02 ordering.
            Box::new(super::trunk_capacity::Migration),
            Box::new(super::trunk_acl_entries::Migration),
            Box::new(super::drop_acl_column::Migration),
    ```

    Do NOT reorder existing entries.
  </action>
  <verify>
    <automated>cargo check -p rustpbx --lib 2>&amp;1 | grep "^error" | head ; awk '/trunk_capacity::Migration/{a=NR} /trunk_acl_entries::Migration/{b=NR} /drop_acl_column::Migration/{c=NR} END{ if (a&amp;&amp;b&amp;&amp;c &amp;&amp; a&lt;b &amp;&amp; b&lt;c) print "order ok"; else print "BAD order a=" a " b=" b " c=" c }' src/models/migration.rs</automated>
  </verify>
  <done>
    - All three Phase 5 migrations appear in Migrator::migrations() in the documented order
    - awk assertion prints "order ok"
    - cargo check -p rustpbx --lib is clean
  </done>
  <acceptance_criteria>
    - `grep -c 'trunk_capacity::Migration' src/models/migration.rs` == 1
    - `grep -c 'trunk_acl_entries::Migration' src/models/migration.rs` == 1
    - `grep -c 'drop_acl_column::Migration' src/models/migration.rs` == 1
    - The awk one-liner in &lt;verify&gt; prints exactly `order ok`
    - `cargo check -p rustpbx --lib` exits 0
    - `cargo test -p rustpbx --lib --no-run` exits 0 (links cleanly)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create stub handler modules (trunk_capacity + trunk_acl) and merge BOTH into api_v1/mod.rs</name>
  <files>src/handler/api_v1/trunk_capacity.rs, src/handler/api_v1/trunk_acl.rs, src/handler/api_v1/mod.rs</files>
  <read_first>
    - src/handler/api_v1/mod.rs (find existing `pub mod trunk_media;` and `.merge(trunk_media::router())` to mirror placement)
    - src/handler/api_v1/trunk_credentials.rs (top-of-file imports — `axum::Router`, `crate::AppState`)
  </read_first>
  <action>
    **Why this task is in Wave 1 (planner note — not for code):** Both Plan 05-02 and Plan
    05-03 want to land in Wave 2 in parallel, but BOTH would need to edit `api_v1/mod.rs`
    to register their module + merge the router. That is a Wave-2 file conflict on a single
    file. We resolve it by creating empty router stubs HERE and registering both in mod.rs
    NOW. Wave 2 plans then ONLY fill in the handler bodies in their respective stub files
    — disjoint files, true parallelism.

    **Stub file 1 — `src/handler/api_v1/trunk_capacity.rs`:**
    ```rust
    //! Stub created by Plan 05-01. Plan 05-02 fills in handlers (TSUB-04 CRUD).
    //! Plan 05-04 wires live observability into the GET handler.

    use axum::Router;
    use crate::AppState;

    pub fn router() -> Router<AppState> {
        Router::new()
    }
    ```
    Match the exact `crate::AppState` import path used by sibling modules (e.g.
    `trunk_credentials.rs`); if the project uses a different path, copy verbatim from
    there.

    **Stub file 2 — `src/handler/api_v1/trunk_acl.rs`:**
    Same shape:
    ```rust
    //! Stub created by Plan 05-01. Plan 05-03 fills in handlers (TSUB-05 CRUD).

    use axum::Router;
    use crate::AppState;

    pub fn router() -> Router<AppState> {
        Router::new()
    }
    ```

    **Modify `src/handler/api_v1/mod.rs`:**
    1. Add module declarations next to the existing Phase 3 sub-resource mods (e.g. next
       to `pub mod trunk_media;`):
       ```rust
       pub mod trunk_capacity;   // Phase 5 — TSUB-04 (handlers in Plan 05-02)
       pub mod trunk_acl;        // Phase 5 — TSUB-05 (handlers in Plan 05-03)
       ```
    2. Add merge calls in the protected Router builder, immediately after the existing
       `.merge(trunk_media::router())` line:
       ```rust
       .merge(trunk_capacity::router())   // Phase 5 — TSUB-04
       .merge(trunk_acl::router())        // Phase 5 — TSUB-05
       ```

    **Critical for Wave 2 plans:** Plan 05-02 and Plan 05-03 MUST NOT edit `mod.rs`.
    They overwrite `trunk_capacity.rs` / `trunk_acl.rs` body with real handlers; the
    `pub fn router()` signature stays the same so the `.merge(...)` calls in mod.rs
    continue to compile.

    DO NOT add this task's stub modules to `files_modified` in Plan 05-02 or 05-03 —
    those plans should only list their own handler file + their test file.
  </action>
  <verify>
    <automated>test -f src/handler/api_v1/trunk_capacity.rs &amp;&amp; test -f src/handler/api_v1/trunk_acl.rs &amp;&amp; grep -q 'pub fn router' src/handler/api_v1/trunk_capacity.rs &amp;&amp; grep -q 'pub fn router' src/handler/api_v1/trunk_acl.rs &amp;&amp; grep -q 'pub mod trunk_capacity' src/handler/api_v1/mod.rs &amp;&amp; grep -q 'pub mod trunk_acl' src/handler/api_v1/mod.rs &amp;&amp; grep -q 'trunk_capacity::router()' src/handler/api_v1/mod.rs &amp;&amp; grep -q 'trunk_acl::router()' src/handler/api_v1/mod.rs &amp;&amp; cargo check -p rustpbx --lib 2>&amp;1 | grep -E '^error' | head &amp;&amp; echo OK</automated>
  </verify>
  <done>
    - Both stub files exist with empty `pub fn router() -> Router&lt;AppState&gt; { Router::new() }`
    - `pub mod trunk_capacity;` and `pub mod trunk_acl;` declared in api_v1/mod.rs
    - Both `.merge(...)` calls present in the protected Router builder
    - `cargo check -p rustpbx --lib` is clean
    - The /api/v1 router compiles end-to-end with the two stubs (no routes registered yet)
  </done>
  <acceptance_criteria>
    - `grep -c 'pub fn router()' src/handler/api_v1/trunk_capacity.rs` == 1
    - `grep -c 'pub fn router()' src/handler/api_v1/trunk_acl.rs` == 1
    - `grep -c 'pub mod trunk_capacity' src/handler/api_v1/mod.rs` == 1
    - `grep -c 'pub mod trunk_acl' src/handler/api_v1/mod.rs` == 1
    - `grep -c 'trunk_capacity::router()' src/handler/api_v1/mod.rs` == 1
    - `grep -c 'trunk_acl::router()' src/handler/api_v1/mod.rs` == 1
    - `cargo check -p rustpbx --lib` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → migration runtime | Operator runs Migrator::up() on production DB; data loss risk if migration is wrong |
| client → /api/v1 stub routers | Stub routers are empty in this plan; no surface area until Plans 05-02/05-03 fill them in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | drop_acl_column migration | mitigate | `has_column` idempotency guard prevents double-drop on rerun; matches Phase 3 D-02 precedent (drop_credentials_column.rs). On a fresh DB the column never exists so the alter is a no-op. |
| T-05-01-02 | Denial of Service | acl_entries unbounded inserts | accept | Per-trunk ACL list growth is operator-driven via authenticated POST (Plan 05-03). UNIQUE (trunk_group_id, rule) caps duplicates; volume bound is operator policy. |
| T-05-01-03 | Information Disclosure | none in this plan | n/a | Schema-only + empty stub routers; no data exposure. ACL rules are not secrets. |
| T-05-01-04 | Tampering | FK CASCADE on capacity/acl | mitigate | CASCADE deletes child rows when parent trunk_group is deleted — prevents orphan rows that would otherwise be loaded during enforcement (defensive: no enforcement runs against a dead trunk_group). |
| T-05-01-05 | Repudiation | created_at/updated_at | accept | Timestamps included (default current_timestamp); no audit trail beyond that for v2.0. |
| T-05-01-06 | Elevation of Privilege | empty stub routers exposing 404 | accept | Empty `Router::new()` registers zero routes; mounting under protected /api/v1 inherits Bearer auth middleware regardless. No surface area until Wave 2. |
</threat_model>

<verification>
- `cargo check -p rustpbx --lib` is clean
- `cargo test -p rustpbx --lib --no-run` links cleanly (compile-only baseline; no Phase 5 tests yet)
- Migration registration order assertion (Task 4 awk one-liner) prints `order ok`
- `grep -r 'Column::Acl\\|pub acl: Option<Json>' src/` returns zero hits
- `grep -q 'pub mod trunk_capacity' src/handler/api_v1/mod.rs && grep -q 'pub mod trunk_acl' src/handler/api_v1/mod.rs` succeeds
</verification>

<success_criteria>
1. `supersip_trunk_capacity` table is created on a fresh DB with the documented columns and FK CASCADE
2. `supersip_trunk_acl_entries` table is created with UNIQUE (trunk_group_id, rule) and position index
3. `rustpbx_trunk_groups.acl` is dropped on Phase-2/3-era DBs and never created on fresh DBs
4. trunk_group::Model compiles without an `acl` field; the rest of src/ has zero references to `Column::Acl`
5. Migrator order is exactly: …, drop_credentials_column, trunk_capacity, trunk_acl_entries, drop_acl_column
6. `src/handler/api_v1/trunk_capacity.rs` and `src/handler/api_v1/trunk_acl.rs` exist as compile-clean stub modules with empty `router()` functions, both declared and merged in `api_v1/mod.rs`
</success_criteria>

<output>
After completion, create `.planning/phases/05-trunk-enforcement-capacity-acl-codec-filter/05-01-SUMMARY.md`
</output>
